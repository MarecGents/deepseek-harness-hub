# PROCESS_QUALITY — dsh-hub 项目流程与质量管理规范

> **本文件是 dsh-hub 的流程与质量管理总纲（SOP + 六西格玛 DMAIC）。** 任何开发任务（迁移、功能、修复、文档）**必须**遵循本规范组织流程；与本文件冲突时，以根 `AGENTS.md` 铁律（不可违反）为准。
> 版本：v1.1 · 2026-08-23 · 状态：生效

---

## 1. 总则

### 1.1 目的
- 将互联网大厂常用的 **SOP（标准作业程序）** 流程化思想与 **六西格玛 DMAIC** 质量管理方法论引入 dsh-hub 开发，使每个阶段有**明确输入/输出/责任人/质量指标/验收门禁**，降低缺陷率、保证可重复、可追溯、可改进。

### 1.2 适用范围
- 本仓库全部开发任务：当前架构维护、插件功能（含 plugins/ 独立插件）、UI/样式、文档、发布。（Tauri 2.x 迁移 M1–M5 已完成）
- 项目级执行细则见《项目执行 SOP》（外部档案仓库 `../docs/process/`），本文档定义方法论与模板。

### 1.3 术语
| 术语 | 含义 |
|---|---|
| **SOP** | Standard Operating Procedure，标准作业程序：把关键流程固化为"谁、在何时、按什么步骤、用什么输入、产出什么、达到什么标准"的可重复作业书 |
| **六西格玛** | 以数据为基础、通过减少过程变异与缺陷（目标 3.4 ppm）实现持续改进的质量管理方法论 |
| **DMAIC** | 六西格玛改进循环：**D**efine 定义 → **M**easure 测量 → **A**nalyze 分析 → **I**mprove 改进 → **C**ontrol 控制 |
| **CTQ** | Critical To Quality，关键质量特性（直接影响用户可感知质量的可度量特性） |
| **主 KPI / 伴随指标** | 阶段主指标（判定阶段达成的单一最权威度量）与伴随质量指标（防主指标被"刷分"的护栏） |
| **门禁** | Gate，阶段/发布前的强制检查点，FAIL 即禁止进入下一步 |

---

## 2. SOP 方法论

### 2.1 SOP 是什么
SOP 把"经验/口头流程"固化为**标准作业书**，让任何执行者（含新 agent）按步骤产出**一致结果**。大厂实践：需求/研发/测试/发布各环节都有 SOP，配套评审会与门禁。

### 2.2 SOP 必备要素（本仓库 SOP 模板的固定字段）
一份合格 SOP 必须包含：

| 要素 | 说明 | 示例 |
|---|---|---|
| **目标** | 本 SOP 要达成的结果（SMART） | "M1 结束产出可运行的 src-tauri 脚手架" |
| **前置条件** | 开始前必须满足 | "P4/P5 核实完成；dev-v2 最新" |
| **输入** | 消费的文档/产物 | 三份迁移报告、REFERENCE.md |
| **步骤** | 编号动作序列（含命令/文件级操作） | "① tauri init … ② 编辑 tauri.conf.json …" |
| **输出** | 本阶段产物清单 | src-tauri/、验收记录 |
| **质量标准** | 输出必须达到的度量 | "窗口可创建；--assemble-only exit 0" |
| **负责人** | 执行主体（agent/人工） | "开发 agent" |
| **异常处理** | 步骤失败/偏差怎么办 | "EBUSY → 退出桌面壳后重试" |
| **验收方法** | 如何判定达标 | "cargo tauri dev 冒烟 + 验收表逐项" |

### 2.3 SOP 编写规范
- 步骤可执行：每条步骤是**动作**（可被 agent 直接执行），不写形容词
- 输入输出显式化：引用具体文件路径/产物名
- 阈值量化：质量标准用数字（"缺陷数 ≤ 3"），不用"尽量"
- 单一事实源：引用既有规范（AGENTS.md 铁律、verify-release 门禁）而非重抄
- 模板见 §8.1

---

## 3. 六西格玛 DMAIC（软件项目适配）

### 3.1 五阶段总览
| 阶段 | 英文 | 制造语境 | **软件项目落地形态（本仓库）** |
|---|---|---|---|
| **D 定义** | Define | 界定项目目标与 CTQ | 明确阶段目标、主 KPI、范围、干系人（用户/agent） |
| **M 测量** | Measure | 测量当前基线 | 建立度量基线：验收表通过率、门禁全绿、缺陷登记数 |
| **A 分析** | Analyze | 找根因 | 缺陷根因分析（5 Why）、门禁 FAIL 原因归类、与三报告/既有行为差异分析 |
| **I 改进** | Improve | 实施改进 | 执行修复/实现，用门禁+验收表验证改进有效 |
| **C 控制** | Control | 固化防回退 | 门禁自动化（verify-*）、回归基线、缺陷登记入库、文档同步 |

### 3.2 软件项目度量体系
- **CTQ 示例**：功能可用性（验收表通过率）、行为一致性（与 dev-v2 当前 rc（如 0.0.2-rc.7）对齐率）、缺陷（缺陷密度/剩余缺陷数）、门禁合规（全绿/FAIL 数）
- **3.4 ppm 的软件化**：不机械套用制造缺陷率；对应为"每阶段发布时**阻断级缺陷 = 0**、遗留低危缺陷显式登记并排期"——零阻断缺陷是硬门禁
- **指标须 SMART**：具体（Specific）、可测（Measurable）、可达（Achievable）、相关（Relevant）、有时限（Time-bound）

### 3.3 DMAIC 在阶段循环中的应用
每个开发阶段（M1–M5）执行一次 DMAIC 小循环：
1. **D**：读《迁移项目执行 SOP》该阶段条目，确认目标与主 KPI
2. **M**：记录基线（当前门禁状态/验收表逐项）
3. **A**：开工前对照三报告任务清单与验收表，识别差异/风险（见 P4/P5 待验证项）
4. **I**：执行 SOP 步骤，逐任务落地
5. **C**：跑阶段门禁（自动化断言 + 人工验收）、登记缺陷、更新验收表状态列

---

## 4. 项目执行 SOP（通用开发流程）

> 项目级阶段化 SOP 见外部档案仓库 `../docs/process/`（如《迁移项目执行SOP》）。本节约定通用流程骨架：

```
需求/目标定义（D）
   ↓ 评审（目标、CTQ、验收标准）
设计/方案（A）
   ↓ 方案评审（三报告/文档引用一致性）
开发实现（I）
   ↓ 代码评审 + 构建 + 门禁
测试/验收（C）
   ↓ 验收表逐项 + 回归基线
发布（verify-release / Tauri 侧 = build:installer 内置完整性预检，见 BUILD.md §5.3）`n   —— 插件双轨验收：独立 npm 发布走 verify-release + 随 hub NSIS 装配冒烟（BUILD.md §7.3）
   ↓ dist-tag / Release 校验（直查 registry，禁缓存）
复盘（缺陷登记、根因分析、SOP 修订）
```

### 4.1 质量门禁清单（每个阶段/发布必查）
| 门禁 | 内容 | FAIL 处置 |
|---|---|---|
| **身份门禁** | 插件身份四重相等（AGENTS.md 铁律 2） | 修复至一致，禁止继续 |
| **构建门禁** | `npm run build && build:client`（或 `cargo build --release` + clippy + test）exit 0 | 修复 |
| **发布门禁** | `node scripts/verify-release.mjs`（插件层）全部 PASS | 禁止 publish（铁律 6） |
| **迁移门禁** | 壳层 `cargo check` + `clippy` + `test` 零告警 + `verify-m4-multi-instance.mjs`（多实例硬验收）；`verify-tauri-release.mjs`（P1–P9）**M5 落地后启用** | 禁止发布 |
| **验收门禁** | 阶段验收表全过（状态列"通过"） | 补齐后重跑 |
| **回归门禁** | 回归基线（dev-v2 当前 rc 行为/上一阶段）无回归 | 修复回归项 |

---

## 5. 阶段化指标框架

### 5.1 指标结构
每个阶段必须定义：
```
主 KPI：1 个（判定阶段达成的权威度量，可自动断言）
伴随质量指标：2-3 个（防"刷分"护栏：缺陷数/门禁全绿/验收表覆盖率）
阈值与验证：达标阈值 + 验证方法（自动化断言/命令/人工）
```

### 5.2 指标定义模板
| 字段 | 内容 |
|---|---|
| 阶段 | M1 / M2 / … |
| 主 KPI | 一句话 + 量化阈值（如"脚手架可运行：`cargo tauri dev` 出窗口，`--assemble-only` exit 0"） |
| 伴随指标 | 缺陷数上限、门禁全绿、验收表通过率 ≥ 100% 或显式豁免项 |
| 验证方法 | 命令/断言/验收表引用 |
| 未达标处置 | 阻断进入下一阶段 / 显式豁免（记录原因+截止） |

---

## 6. 质量控制机制

### 6.1 缺陷管理
- 每个阶段维护**缺陷登记**（编号、严重度、现象、根因(5 Why)、修复、验证、关闭）
- 阻断级缺陷（Block）**禁止带病发布**；高（Major）须发布前修复或显式豁免；中低（Minor/Low）登记排期

### 6.2 根因分析
- 门禁 FAIL / 缺陷重复出现 → 5 Why 追溯根因（如 rc.10-13 全新环境崩溃 → 根因：发布验证缺"干净安装"环节 → 对策：verify-release P4）
- 根因对策必须**固化进流程/门禁**，而非一次性修复（对照 docs/关键踩坑记录.md 的"正确做法"）

### 6.3 回归基线
- dev-v2 当前 rc（如 0.0.2-rc.7）行为 = 回归基线；rc.14 仅作 WebView2 历史对照；每阶段验收表增量覆盖
- 每次发布/里程碑前跑回归，发现回归即阻断

### 6.4 评审
- 方案级评审（与三报告/文档一致性）、代码级评审（AGENTS.md §4）、流程级评审（本规范适用的文档变更）
- 评审意见闭环：意见 → 修订 → 复核，直至通过（多轮可）

---

## 7. 与既有规范的关系

| 规范 | 关系 |
|---|---|
| `AGENTS.md`（总纲 + 分层 harness） | **铁律优先**；本规范不得削弱铁律（门控/多实例/身份一致/发布门禁/隔离） |
| `docs/关键踩坑记录.md` | 缺陷根因与"正确做法"的来源库；新坑必补录 |
| `scripts/verify-release.mjs` | 发布门禁的自动化实现（P1-P5） |
| 三份迁移报告（docs/tauri-migration/） | 《迁移项目执行SOP》的任务/验收事实源 |
| 分支策略（AGENTS.md §5.1） | dev-v2 开发 → PR → main；dev-v1 冻结 |

---

## 8. 模板附录

### 8.1 SOP 文档模板
```markdown
# SOP：<阶段/环节名>
- 目标：<SMART 目标>
- 前置条件：<必须满足项>
- 输入：<文件/产物引用>
- 步骤：
  1. <动作（可执行）>
  2. ...
- 输出：<产物清单>
- 质量标准：<量化阈值>
- 负责人：<执行主体>
- 异常处理：<常见失败与处置>
- 验收方法：<命令/断言/验收表引用>
```

### 8.2 阶段验收表模板
```markdown
| # | 验收项 | 验收方法 | 平台 | 状态 | 备注 |
|---|---|---|---|---|---|
| 1 | <行为/能力> | <命令/断言> | Win/mac/Linux | ☐ | <豁免原因> |
```

### 8.3 DMAIC 工作表模板
```markdown
| D 定义 | 目标=…；主 KPI=…；CTQ=… |
| M 测量 | 基线：验收表 X/Y 过；门禁 Z 绿 |
| A 分析 | 差异/风险：…；根因：… |
| I 改进 | 任务：T.x.x…；变更：… |
| C 控制 | 门禁：…；回归：…；缺陷登记：… |
```
