# BUG_FIX_SOP — dsh-hub 缺陷修复标准作业程序

> **目的**：让每次 bug 修复都能「快速、准确、对症下药」——终结无厘头反复试错。本文把所有 bug 修复经验（含 2026-08-27 会话重命名三易其稿、右键菜单多轮迭代）固化为强制流程。
>
> **适用**：所有 bug 反馈处理（用户口述现象/自动化报告/人工发现）。
>
> **上位文档（优先级高于本文）**：根 [AGENTS.md](AGENTS.md)（铁律为先）→ [PROCESS_QUALITY.md](PROCESS_QUALITY.md)（DMAIC 方法论总纲；本文是其缺陷修复细则）。
> **陷阱索引**：[docs/关键踩坑记录.md](docs/关键踩坑记录.md)（#N 编号即本文引用点）。

## 0. 铁则（违反即流程 FAIL）

- **R1 数据安全**：复现/验证一律隔离环境（`DSH_HOME` 指向 `%TEMP%` 独立目录）；真实 `~/.dsh` **零触碰**（根 AGENTS.md 铁律 8）。
- **R2 禁止臆测驱动修复**：先测量、后下结论。凡「点击无反应 / 改了没效果 / 现象没复现」——**必须先证明事件/代码实际到达了哪一步**（链路打点、日志、判别实验），不得凭「应该是…」直接改代码。
- **R3 破坏性操作先确认身份**：删文件/覆盖配置/杀进程前，核对目标路径/进程归属与影响面（参考铁律 8 删除纪律）。
- **R4 修复后必须全量门禁 + 隔离实例验证 + 提交**：tsc / build:client（或 cargo fmt/clippy/test）、lib 零漂移、verify-release（发布场景）、实例验证、按分支纪律提交（当前 dev-v2）。
- **R5 经验必须沉淀**：每次修复把「现象→测量→根因→方案→验证」写入 `docs/关键踩坑记录.md`（新 # 或更新旧条目），并同步受影响文档（README/FUNCTIONS/AGENTS）。

## 1. 阶段一：复现与信息收集（Define）

- **输入**：用户反馈或报告（现象、触发位置、触发步骤、环境）。
- **动作**：
  1. 隔离环境复现；无法复现则向用户索取「最小复现步骤 + 截图/日志」，并记录 tentative 不可复现说明。
  2. 记录环境要素：版本/分支、安装方式（NSIS/dev/沙箱）、会话数据量、其他线索（日志尾部、diag_report）。
  3. 现象归类（决定测量策略）：崩溃/白屏/无反应/错乱/性能/竞态/仅在特定数据量出现。
- **门禁**：存在一致可复现路径，或明确的不可复现说明（含用户侧补充）。

## 2. 阶段二：测量取证（Measure）——本 SOP 的差异化重点

原则：**不猜测，量化每一跳**。手段按成本升序使用：

1. **读日志**：dsh-hub Rust 日志（`%TEMP%/<沙箱>/dsh-hub-ui-*.log`）、沙箱 `dsh-hub/logs/`、host `dsh-stdout`——先看崩溃/错误/告警。
2. **静态盘点**：grep 事件注册点（`addEventListener('contextmenu'/'click'…)`）、状态机、DOM 锚点；对照 dsh 官方源码（REFERENCE.md 索引）核实结构契约（role/data-slot/class）。
3. **隔离判别实验**：改一个变量对比行为（禁用某功能/切另一数据量/直连官方入口），快速证伪假设。
4. **链路探针（首选取证利器）**：页面侧行为不进 Rust 日志时，临时在关键路径打点：
   - host 加一次性 GET `/api/dsh-hub/ctx-trace?t=<desc>`（`console.log` → `dsh-stdout` 日志可见）；
   - 页面在每一步 `fetch(...ctx-trace...)` 打点（如 `btn→clicked→events→menu→dialog`）；
   - **打完即删**：探针属临时诊断，提交前必须移除（含 host 路由）。
5. **用户判别项**（10 秒可答的）:交给用户，最快排除环境/版本级根因——例：「直接点官方 ⋯ 按钮能弹菜单吗？」（#85 用此判别锁定「官方弹层整体不可用」）。

- **输出**：测量记录（每步事实 + 时间 + 上下文）。
- **门禁**：候选根因均有测量数据支撑，或已列出证伪的假设及其证据。

## 3. 阶段三：根因确定（Analyze）

判定根因需同时满足**三要素**：

1. **机制完整**：从现象到根因一步不跳（若需「应该是/可能」衔接，继续测量）。
2. **与测量一致**：全部测量点都符合该解释（含负向证据）。
3. **排除替代**：至少 1–2 个替代假设已被明确证伪。

常用根因范式与踩坑索引对照：

| 范式 | 典型症状 | 踩坑 |
|---|---|---|
| 浏览器原生行为无法被 DOM 拦截 | 右键/默认行为不可控 | #77 |
| 全局处理器重复注册 | 双重菜单/双重执行 | #78 |
| 同节点多监听全部执行、preventDefault 无效于他键 | 菜单叠加/事件兜底误触 | #79 |
| 豁免/白名单过宽或锚点错位 | 该出现的不出现 | #80、#73 |
| 模块顶层 TDZ（词典/常量生成成函数调用） | 插件装载崩 | #81 |
| 去重缓存锁死早期失败态 | 首帧失败永不复原 | #82 |
| 官方弹层不可达（合成事件/真实事件均失败） | 依赖官方 UI 入口的功能失效 | #85 |
| 依赖项未打包/双半不同步（lib 未重建） | 改了没效果 | #6、#74 |

> 特别提醒：**「改了没效果」先查两条**——① `build:client` 是否漏跑（lib/ 未进新 bundle）；② 实例是否加载新代码（探针证活或产物时间戳）。

## 4. 阶段四：方案设计与审查（Improve）

**设计准则**：
- **优先官方数据层 API/能力**（如 `sessions.binding(id).session.rename`）：同源保证左右联动，且不依赖可能不可达的官方弹层——**采用任何官方 UI 链前先判别其真实可用性**（真实点击）。
- 注入式微调（官方结构内），**禁止自造布局**（用户红线；参考设置卡画廊回退教训）。
- 事件/弹层：合成事件驱动第三方 React 弹层在本环境**不可靠（实测）**——方案要么避免、要么带层内兜底。
- 并发/竞态：异步结果必须按序裁决（B7 模式）、失败保留现场可重试。

**强制审查**：常规 ≥ **3 轮**（正确性/时序与竞态/安全与副作用）；高风险（官方 UI、竞态、多端、卸载/安装链路）≥ **5 轮**（再加：兼容与多语言、回归与降级）。每轮审查产出一个已回答问题或证伪点。

**全流程事件模拟（必须）**：按时间轴列出事件链（t=0 触发 → React/原生渲染 → 用户/程序动作 → 结果），标注每步依赖、可信度假设、失败降级路径；无死链（每步有明确触发与失败出路）方可进入实现。

## 5. 阶段五：实施与验证（Improve / Control）

1. 实现 → 门禁（tsc / build:client；Rust 侧 cargo fmt/clippy/test；lib 零漂移）。
2. 隔离实例验证（按验收点逐项；行为性 bug 优先用户实操确认）。
3. **清理临时探针**（含 host 路由与页面 fetch）→ 门禁复跑确认干净。
4. 更新 `docs/关键踩坑记录.md`（新增/修订 #N）与受影响文档。
5. 提交（分支纪律：dev-v2；按用户指示才推送）。
6. 回归速览：相邻功能（如会话菜单/标签栏/右键各态）过一遍。
- **最终门禁**：用户确认 + 工作区干净 + 提交完成 + 踩坑登记。

## 6. 快速决策树（高频取舍）

| 场景 | 优先动作 |
|---|---|
| 点击无反应 | 链路探针量化（事件到哪一步停止）+ 用户判别官方入口可用性 |
| 菜单/弹层异常 | 先判别官方入口是否可用（真实点击）→ 可用走官方链；不可用走「官方数据层 + 自收敛 UI」 |
| 改了没效果 | 先查 `build:client` 是否漏跑、实例是否加载新 bundle（探针/时间戳） |
| 空白/显示问题 | 先盘点 DOM 结构（官方源码 role/data-slot）+ 豁免/锚点最小集 |
| 崩溃/装载失败 | 日志 + 模块顶层顺序审计（TDZ/未定义）+ 模拟装载 |

## 7. 报告模板（每次 bug 必输）

```
现象：<用户复述 + 环境>
环境：<分支/版本/安装方式/沙箱/数据量>
测量：<步骤与数据（探针/日志/判别实验结论）>
根因：<一句话 + 触发条件 + 排除的替代假设>
方案：<采用方案 + 审查轮数与结论 + 事件链模拟结论 + 备选/>
验证：<门禁结果/实例验证点/用户确认>
踩坑：<docs/关键踩坑记录.md #N 新增或更新>
```