# 审查意见：迁移项目执行SOP 与三报告/既有门禁的一致性审查

> 审查对象：`docs/process/迁移项目执行SOP-2026-08-17.md`（主审）
> 对照事实源：《可行性分析报告-2026-08-17.md》《代码迁移方案报告-2026-08-17.md》《代码落地方案报告-2026-08-17.md》、`PROCESS_QUALITY.md`、根 `AGENTS.md`（铁律）
> 审查日期：2026-08-17 · 审查角度：任务号 / 验收表 / 门禁 / 口径 / PROCESS_QUALITY 合规 / 决策点与风险清单
> 纪律：以下每条均基于实际读到的文件内容；关键断言（P1–P8 来源、P2/P6 缺失、拍板矛盾等）已用全文检索复核。

---

## 一、问题清单（按严重度排序）

严重度定义：**高** = 阻断级矛盾；**中** = 流程/可追溯性缺陷，需修订后执行；**低** = 措辞、枚举、口径细节。

### 1.【中】§10 对账「前置 P1–P8 与落地报告逐字一致」来源错标，且 P2/P6 未映射
- **涉及小节**：SOP §10（L422）、§2.2/§3.2/§4.2/§5.2/§6.2 前置条件；对照《可行性分析报告》§7.2、《代码落地方案报告》§10/附录。
- **描述**：P1–P8 是《可行性分析报告》§7.2 的前置条件清单；《代码落地方案报告》通篇**没有** P1–P8 编号清单（仅行文中引用 P2/P3/P4/P5，见 T5.4「P3 前先用测试证书」、T1.3/T1.4「P4/P5 核实」、附录 C「P2 未同步诊断参数」）。SOP 自身 §2.3（L37）、§6.3（L287）其实也按「可行性 §7.2」引用，§10 却声称 P1–P8 与落地报告「逐字一致」——来源错标。同时，可行性 P2（Node sidecar 打包验证：三端可跑 `dsh web --port 0` + 体积实测）与 P6（M1/M2 最小闭环）在 SOP 全文**无任何里程碑承接/验收引用**（grep 复核：SOP 无前置 P2/P6 出现），与「前置 P1–P8 一致」的自我声明相悖。
- **修改建议**：① §10 改为「任务号 T1.1–T5.7、横切 X1–X4、验收 §9 行 1–28 与《代码落地方案报告》一致；前置 P1–P8 与《可行性分析报告》§7.2 一致」；② 显式补 P2/P6 映射：P2 收口于 T5.2（node 三端打包 + 供应链校验）与 T5.5-P3（安装逐端冒烟隐含 sidecar 三端可跑）；P6 = M1/M2 里程碑本体（像素验证项已见于 §3.9），建议在 §2.2 前置条件加一行说明；③ 「验收 §9 行 1–28 逐字一致」措辞过强（SOP 未复现 28 行表，仅为行号引用+摘要），建议改「行号引用一致」。

### 2.【中】P 前缀双义：可行性前置 P1–P8 与 verify-tauri-release 发布门禁 P1–P9 同名碰撞
- **涉及小节**：§2.2（P1 桥选型）、§6.2（P3 签名/P7 双通道/P8 门禁扩展）与 §6.4/§6.6/§6.8/§6.9（门禁 P1–P9）、§2.9（门禁 P1）。
- **描述**：M5 章节内「P3 签名与更新基础设施就绪」（§6.2 前置）与「P3 安装产物逐端冒烟」（§6.4 门禁）、「P4 更新闭环」（§6.4）与「P4/P9 门禁 FAIL 禁止跳过」（§6.8）同前缀并存；§2.2「P1 桥选型冻结」与 §2.9「verify-tauri-release P1 的 clippy/test 基线」亦然。同一编号两义，读者无法仅凭编号区分「前置条件」还是「发布门禁」。
- **修改建议**：前置条件改标 PR1–PR8（或 C1–C8），发布门禁保留 P1–P9；在 §10 对账注明命名约定，避免后续执行混淆。

### 3.【中】决策拍板权自相矛盾（D3 声音通道、D7 sidecar 通道）
- **涉及小节**：§4.7（L184）、§6.7（L327）与附录 B 拍板规则（L395）。
- **描述**：附录 B 拍板规则将 D3（声音三端通道）、D7（sidecar 通道 A/B/C）划为「技术权衡类，由执行 agent 出具决策记录」；但 §4.7 明确「**声音通道决策须人工确认**」，§6.7 明确「**P7 双通道决策须组织/用户拍板**（可行性 §5.3 为组织决策项）」。同一文档对同一决策点的拍板主体给出相反规定，执行时会产生「agent 可否自决」的歧义。
- **修改建议**：统一口径——D3/D7 按 §4.7/§6.7 升级为「须人工/组织确认」类（D7 引可行性 §5.3 组织决策属性），附录 B 拍板规则删除或改注 D3/D7。

### 4.【低】X4-① capabilities 终审时机：M4 与 M5 矛盾
- **涉及小节**：§1 横切（L21「T1.4 最小授权 → M4 X4-④ 桥鉴权 → M5 终审」）vs §5.5 M4 输出（L240「X4-① capabilities 终审产物」）vs 落地 X4-①（「M4 用 tauri dev ACL 报错驱动逐项最小化」「X4 产物 = capabilities 最终清单…」）。
- **描述**：§1 横切写「M5 终审」，§5.5 与落地报告均为 M4 收尾/产出终审产物，自相矛盾。
- **修改建议**：§1 改「T1.4 最小授权 → M4 X4-① 终审 + X4-④ 桥鉴权 → M5 发布前安全复核」。

### 5.【低】M2 主 KPI 枚举 6/7，缺「居中」
- **涉及小节**：§1 阶段总览表 M2 行（L16）vs §3.6 质量标准（L121）。
- **描述**：总览表括号内仅列 6 项（DPI 双屏像素一致、最小尺寸无 0x0 退化、最大化恢复、主题三态截图、老 window-state 兼容读、设置卡链路就绪），缺 §3.4 对比表第 3 行「居中」，却声称「7 项全部通过」。
- **修改建议**：括号内补「居中」项。

### 6.【低】M3 验收方法引用 §9 行 17（崩溃重启）过早
- **涉及小节**：§4.9（L193）。
- **描述**：落地 §9 行 17 里程碑为 M4/M5（node.rs 重启循环属 T4.1）；M3 仅 T3.6 quit.marker 接口先行，无重启循环实现，M3 验收引用行 17 过宽。
- **修改建议**：M3 仅验 quit.marker 语义，行 17 改注「重启循环 M4 验」或从 M3 引用中移除。

### 7.【低】附录 B：D10 与 D3 截止/默认互斥
- **涉及小节**：附录 B D3（L386）/D10（L393）。
- **描述**：D10「声音/通知层归属，M3 定案，默认保留 Node 侧（sidecar 内播放）」与 D3「三端通道 M3/M4 前闭合、默认待定」存在顺序冲突：若 D3 最终选 HTMLAudio（页面播放）或 Rust rodio（壳内播放），D10 的「保留 Node 侧」默认即不成立。
- **修改建议**：D10 截止并至 M3/M4，默认值注明「仅当 D3 选平台命令（c）或保留 sound.ts 时成立」。

### 8.【低】附录 C 标题「P4/P5 待验证项」欠准确
- **涉及小节**：§9 附录 C（L399）。
- **描述**：V4–V12 来源为落地 §10-2/§10-3/§10-1、迁移 §9.2-11、T5.2/T5.4、X4-⑦，并非全部归属 P4/P5；标题以偏概全。
- **修改建议**：改题「待验证项与核实窗口」或按来源（P4/P5/§10-x/T5.x/X4-⑦）分组标注。

### 9.【低】M1 步骤未列 T1.9（无操作任务）
- **涉及小节**：§2.4 步骤 1–10（L39-49）vs §2.3 输入（L37）/§2.10 DMAIC（L84）。
- **描述**：步骤序列只到 T1.8 + X1，T1.9（技术路线文档「不改」）仅在输入与 DMAIC 出现；与「任务号严格一致」声明相比少一步。
- **修改建议**：步骤表补「T1.9 无操作（登记确认不改技术路线文档）」或注明 T1.9 为无操作任务。

### 10.【低】M1 字段核对 8/8 清单与落地 §2.3-2 字段清单不完全一致（缺 frontendDist/devUrl）
- **涉及小节**：§2.6（L61）、T1.3（L42）vs 落地 T1.3/§2.3-2。
- **描述**：SOP 8/8 清单为 identifier / productName / version / decorations=false / transparent=false / min 480×360 / externalBin 预留位 / webviewInstallMode；落地字段核对另含 `frontendDist/devUrl`，SOP 的 8 项与 T1.3 步骤均未列出。
- **修改建议**：8/8 清单补 frontendDist/devUrl（或注明「等」覆盖），T1.3 步骤补两个字段。

### 11.【低】PROCESS_QUALITY §4.1「迁移门禁（壳层，M4 起）P1-P9」与 SOP 启用时机口径不一致
- **涉及小节**：PROCESS_QUALITY §4.1 vs SOP §2.9（L78「clippy/test 自 M1 起建立基线，M5 全量启用」）/§6.4（T5.5 脚本 M5 才创建）。
- **描述**：PROCESS_QUALITY 表述迁移门禁「M4 起」全量执行，SOP 实际为「M1–M4 建基线、M5 全量启用」（脚本本身 M5 落盘）。
- **修改建议**：二选一对齐——PROCESS_QUALITY 改「M5 起全量、M1–M4 基线建立」，或 SOP 注明「M4 起按 PROCESS_QUALITY 门禁精神预演（脚本 T5.5 落盘）」。

### 12.【低】§1 铁律「插件层零改动」未带 T4.9 例外
- **涉及小节**：§1 铁律（L7）vs §5.6 断言（L245「T4.9 桥端点同层扩展除外」）vs 落地 T4.9。
- **描述**：§1 说「src/client/*、config/workspace/pins/backgrounds API、cordis.patch.yml 零改动是全程硬约束」，未注明「桥端点同层扩展（index.ts 扩展或新增 bridge-server.ts）除外」；与落地 T4.9「插件层 API 原样 + 桥端点同层扩展」的放宽一致，但措辞未同步，易被误读为 index.ts 也不可动。
- **修改建议**：§1 铁律补「（T4.9 桥端点同层扩展除外）」。

---

## 二、亮点

1. **任务号体系完整对齐，无独有/缺失**：T1.1–T1.9 / T2.1–T2.6 / T3.1–T3.7 / T4.1–T4.9 / T5.1–T5.7 全部与落地报告一一对应（T1.9 为无操作任务），X1–X4（含 X4-①/④/⑥/⑦）与落地 §7 横切一致；SOP 未发明任何新任务号。
2. **验收表 28 行全覆盖，关键硬验收无一遗漏**：行 1–28 在各里程碑均有映射（M1:22/23/28；M2:1/2/3/6/23/28；M3:4/5/6/7/8/15/17/24；M4:9-18/22/23/26/27/28；M5:19/20/21/24/25/27）。AppUserModelID（行 24，M3/M5）、启动屏（行 23，M1/M2/M4）、多实例硬验收（M4 §5.4-8/§5.9 + M5-P8）、卸载冒烟三端（M5-P6 ⑥b，Windows /S / macOS .app / Linux AppImage + `~/.local/share/<identifier>`）均齐备。
3. **verify-tauri-release P1–P9 ↔ 落地 T5.5 门禁 ①–⑨ 逐项同构**：P1 构建 ↔ ①、P2 装配冒烟 ↔ ②、P3 安装逐端 ↔ ③（含 Win10 无 WebView2 Runtime、安全软件拦截用例）、P4 更新闭环+发布后直查 ↔ ④、P5 身份四重相等 ↔ ⑤、P6 卸载冒烟+逐端 ↔ ⑥/⑥b、P7 回退全链路 ↔ ⑦、P8 多实例硬验收 ↔ ⑧、P9 退役门禁+哈希登记 ↔ ⑨。与 AGENTS.md 铁律无冲突：身份四重（铁律 2）、`DSH_HUB_LAUNCHED=1` 门控（铁律 3，T4.1/M4 验收）、多实例默认拒共存（铁律 4，T4.2 红线 + M4 硬验收 + P8）、发布门禁全 PASS 禁止跳过（铁律 6）、铁律 8 改写为「测试机装安装器首启冒烟」（T5.6）。
4. **关键口径全部继承审查轮 2 收敛结果，与三报告一致**：桥方向标签（notify/applyTheme/applySize 为 sidecar→壳 stdout 上行请求行、quit 为壳→sidecar stdin 下行、READY/output/exit 为 stdout 事件行，§2.2/§4.4/§5.4）；体积双口径（壳二进制 ~10MB 级 + 安装包 40-80MB 级，§1/§6.6/附录 A，删除「安装包 ~10MB」硬验收）；`--port 0` 随机端口 + stdout 读端口（T1.1/T4.1，不引入 portpicker）；`$DSH_HOME` env trim 后非空 || `home_dir()/.dsh`、不用 `data_dir()`、单测覆盖 `DSH_HOME=' '`（T2.2）；Cargo 恒 0.0.0 + version 单一来源（T1.1/T1.3）；`DSH_HUB_PACKAGE_ROOT`、帧前缀 + schema 严格校验 + READY「先验证再导航」（T4.1/T4.4）。
5. **PROCESS_QUALITY 合规度高**：每里程碑完整包含 §8.1 九要素（目标/前置/输入/步骤/输出/质量标准/负责人/异常/验收）；附录 A 指标总表遵循 §5.2 模板（阶段/主 KPI/伴随指标/阈值/验证方法/未达标处置）；每里程碑含 §8.3 DMAIC 表；「阻断级缺陷 = 0 硬门禁」照搬 §3.2/§6.1；验收方法以命令出口/自动断言优先。
6. **决策点 D1–D10 与风险 V1–V12 全部可溯源**：D1–D10 依据均指向落地 §10/§9/T2.6/T4.8、迁移 §3.2/§7.1/§7.5-②/§3.1 或可行性 §2#15，截止阶段总体合理；V1–V12 分别对到 P4/P5/落地 §10/迁移 §9.2/T5.2/T5.4/X4-⑦。SOP 还妥善处理了可行性 P6「全部满足方可启动 M1」的内部时序矛盾（P6 实为 M1/M2 产物）——摊入里程碑门禁是正确工程判断。
7. **M1 首批 PR 清理三报告遗留措辞**（迁移 §1.2「端口预选」、可行性 #21「或 Rust 首启调用一次」）正确承接落地附录 D ARCH-残留①②，闭环完整。

---

## 三、结论

SOP 与三份迁移报告及既有门禁**总体高度一致，无阻断级（高）矛盾**：任务号、验收表 28 行、verify-tauri-release P1–P9 门禁、关键口径（桥方向/体积双口径/--port 0/$DSH_HOME/Cargo 恒 0.0.0）均对齐，PROCESS_QUALITY 模板合规。存在 **3 项中等问题**需修订后执行：① §10 对账把 P1–P8 来源错标为落地报告且 P2/P6 无映射；② 前置 P1–P8 与发布门禁 P1–P9 同名前缀碰撞（M5 章内尤甚）；③ D3/D7 决策拍板权在附录 B 与 §4.7/§6.7 之间自相矛盾。另有 9 项低级措辞/枚举/时机细节（X4-① 终审时机、M2 主 KPI 枚举缺居中、M3 引用行 17 过早、D10/D3 互斥、附录 C 标题、T1.9 无步骤、字段核对缺 frontendDist/devUrl、迁移门禁启用时机口径、§1 铁律 T4.9 例外措辞）。建议按问题 1–3 修订后再作为 M1–M5 执行基准发布，问题 4–12 随 M1 首批 PR 一并清理。

---

## 附：逐项核对速览

| 审查角度 | 结果 |
|---|---|
| 任务号（T/X） | ✅ 全部对齐，无独有/缺失（T1.9 无操作） |
| 前置 P1–P8 | ⚠️ 来源错标（应为可行性 §7.2）；P2/P6 未映射 |
| 验收表 28 行 | ✅ 全覆盖；AppUserModelID/启动屏/多实例硬验收/卸载冒烟三端均在列 |
| 发布门禁 P1–P9 | ✅ 与落地 T5.5 ①–⑨ 同构；与铁律 2/3/4/6 无冲突 |
| 关键口径 | ✅ 桥方向/体积双口径/--port 0/$DSH_HOME/Cargo 0.0.0 一致 |
| PROCESS_QUALITY | ✅ §2/§5/§8 模板合规（1 处门禁启用时机措辞差异，见问题 11） |
| 决策点 D1–D10 / 风险 V1–V12 | ⚠️ 可溯源、截止总体合理（D3/D7 拍板矛盾、D10/D3 互斥见问题 3/7） |
