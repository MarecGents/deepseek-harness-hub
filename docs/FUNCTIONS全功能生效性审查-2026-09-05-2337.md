# FUNCTIONS.md 全功能生效性审查-2026-09-05-2337

## 任务概述

- **名称**：FUNCTIONS.md 全功能生效性审查（dsh-hub v0.1.6 / dsh 0.1.2-rc.1 适配后）
- **目标**：核验 FUNCTIONS.md（A–K 区）全部功能在当前代码中真实接线生效，未被 0.1.2-rc.1 适配（F1–F4）破坏
- **范围**：deepseek-harness-hub @ dev-v2 `74182e4`（只读审查，零代码改动）
- **方法**：4× 并行 Explore 子代理分区静态审查（A/K、B/C/D、E/F/G、H/I/J）+ 主进程编译门禁复跑 + 可疑项亲自交叉验证 + 同 HEAD 运行时证据引用

## 执行过程摘要

1. **基线确认**：HEAD 74182e4（v0.1.6），工作树净；FUNCTIONS.md 基线标注仍为 v0.1.5（2026-09-03）。
2. **并行静态审查**（4 子代理，每项核验链：功能→实现文件→装配接线→适配影响→测试覆盖）：
   - A/K 区 18 项：全部接线确认。launch-token 链完整（parse_dsh_ready_port 解析/NodeState 存储/首启+崩溃重启两处导航拼接/单测）；A7 DSH_CMD 帧协议两侧逐字段一致；K1 门控/K3 fail-closed 多实例/K7 S0 三层防护（Host/Origin/token 常量时间）均活。
   - B/C/D 区 12 项：11 项接线确认。client/index.ts 12 个挂载点全部在位；F2 sessionId 适配链完整（SlotMap 无 owner → 标准 prop → effect 依赖重取）；D3 F4 迁移后服务守卫降级不变。
   - E/F/G 区 15 项：全部接线确认。F1 settings 迁移完整（旧 API 清零；新签名与 dsh-settings@0.1.2-rc.1 逐参匹配；inject:['settings'] 必要性确认；config-api→config-store→onChange 活应用链未受损）；E/G 区文件未被适配提交波及。
   - H/I/J 区 10 项：全部接线确认。node_modules 无 dsh-client-runtime 残留；I2 消费的 ctx.tools.guard 在 dsh-tools 0.1.2-rc.1 实证存在；4 插件 cordis.patch.yml/装配/资源分发三段齐全。
3. **编译门禁复跑**：tsc ✅ · build:client ✅ · lib/ 零漂移 ✅ · cargo fmt ✅ · clippy -D warnings ✅ · cargo test ✅ · 插件测试 **115/115** ✅（node --test 直跑）。
4. **交叉验证**（主进程亲自复核）：
   - D5「添加到当前任务/在辅助对话中提问」：确认两菜单项 action 均为 `copyText(selectedText)` 占位（context-menu.ts:159-166）。
   - G6「四段独立开关」：确认仅一个 `soundEnabled` 总开关（shell-config.ts:35、settings-card.tsx:500 单 checkbox）。

## 关键发现与决策

- **55 项功能 0 接线断裂**；a219b6a（0.1.2-rc.1 适配）未破坏任何 FUNCTIONS 项，diff 精确且多项带防回归单测。
- **9 条非阻断发现**（详见下表），其中 2 条为功能实质缺口（D5 占位动作、G6 开关数），4 条为文档漂移，其余为卫生/工程化问题。
- 决策：本任务为只读审查，全部发现以报告形式交付，不改代码与文档；修复待用户指示。

## 发现清单（按优先级）

| # | 级别 | 发现 | 位置 |
|---|------|------|------|
| 1 | 功能缺口 | D5 选中文本菜单的「添加到当前任务」「在辅助对话中提问」只复制文本，未实现实质行为 | src/client/context-menu.ts:159-166 |
| 2 | 功能缺口 | G6 提示音非「四段独立开关」，实为单 soundEnabled 总开关 + 四段独立触发 | src/models/shell-config.ts:35 |
| 3 | 文档漂移 | 右键菜单优先级：代码为 对象行→编辑→链接→选中→空白；AGENTS.md:126/FUNCTIONS.md D5 仍为旧顺序 | src/client/index.ts:466-505 |
| 4 | 文档漂移 | A4 已是纯 3/4 无上限（1600×1000 上限已移除），FUNCTIONS.md 未同步 | src-tauri/src/window.rs:89-104 |
| 5 | 文档漂移 | C3 表头「11 动作」与枚举 10 项口径不一（条件渲染菜单） | FUNCTIONS.md C3 |
| 6 | 依赖陈旧 | dsh-usage-stats peerDeps：dsh-session-persistence 已消失于 rc.1 生态；dsh-host-webserver ^0.1.0-rc.6 不匹配 0.1.2-rc.1——npm 独立轨安装有风险（hub junction 轨无害） | plugins/dsh-usage-stats/package.json |
| 7 | 工程化 | 4 插件 115 个 node:test 用例无 npm test 入口（需 node --test 直跑；目录参数调用会 MODULE_NOT_FOUND，须显式传文件） | plugins/*/test/ |
| 8 | 卫生 | token.ts:11/api-auth.ts:4 注释陈旧（tapIndex→index-inject）；dev/index.html UTF-16LE 编码异味；AGENTS.md §5.4 的 DSH_HUB_ASSEMBLE_ONLY 为历史残留（现行为 CLI --assemble-only） | 多处 |
| 9 | 待定夺 | 未跟踪文件 src-tauri/.github/workflows/installer.yml（T1 手动 NSIS 工作流）未提交 | src-tauri/.github/ |

## 遇到的问题与解决

| 问题 | 解决 |
|------|------|
| cargo clippy 首跑报 usage 错误 | shell 状态不跨调用，cwd 停在仓库根——cd src-tauri 后用 `cargo clippy --all-targets -- -D warnings` 重跑通过 |
| 插件测试目录参数调用全部 MODULE_NOT_FOUND | node --test 目录参数在本机解析异常；改为显式传 4 个 index.test.mjs 文件路径，115/115 通过 |

## 最终结果与状态

- **结论：FUNCTIONS.md A–K 全部 55 项功能接线生效，无断裂项；门禁与测试全绿。**
- FUNCTIONS.md 中自标 🔶/⚠️ 的条目（真机回归类：拖拽排序、多屏、提示音听感、崩溃重启演练、K5 装配自愈等）维持其原标注——本次为静态接线 + 门禁审查，不推翻也未升级这些标注。
- 静态审查边界：K6 安装完成页桌面快捷方式依赖 Tauri NSIS 官方模板行为；D3 官方 slot 声明在 dsh 应用 bundle 内静态不可读——两项均以装配链 + 同类成功先例佐证。
- 运行时证据（同 HEAD）：v0.1.6 T1 verify-release ALL PASS + NSIS 安装包（SHA256 aff75a7e…）+ 隔离冒烟（token 导航/认证/UI 渲染）。
- 建议（待指示）：① 补全 D5 两动作实质行为或将 FUNCTIONS.md 标注降级；② G6 实现四独立开关或改文档；③ 同步 4 处文档漂移；④ 升级 I1 peerDeps；⑤ 固化插件测试 npm script。
