# 壳功能精简：右侧栏死代码与自研终端栈退役

日期：2026-09-23 ｜ 分支：fix/0.1.7-and-subagent-sound

## 起因

内核升到 0.1.7-alpha.2 后提出「壳里是不是有些功能可以删了」。逐项对位官方 276 个包后，
结论是**能确定的死代码只有一处**，终端与权限属形态差异而非纯冗余。

## 评估结论（对位表）

| 壳功能 | 代码量 | 官方对位 | 判定 |
|---|---|---|---|
| 右侧栏 right-sidebar | 1426 行 | sidebar-right / -files / -browser / -documentpreview | **死代码**（2026-09-18 起未 import） |
| 底部终端 dock | 1965 行 | dsh-terminal + ui-sidebar-terminal（右侧栏 tab） | 重叠·形态不同 |
| 权限 chip | 170 行 | permission-presets（设置默认 + /permission 弹窗） | 重叠·形态不同 |
| 皮肤 skins | 1669 行 | theme（仅 light/dark/system） | 壳独有·增值 |
| 置顶会话 | 1245 行 | 无 | 壳独有 |
| 会话导航栏 rail | 668 行 | 无 | 壳独有 |
| 模型选择器 | 748 行 | model-selection（官方无供应商面板） | 刻意覆盖·不可删 |

host 半包 29 个模块**全部可达**，零孤儿。

## 决策与执行

1. **删右侧栏死代码** — 已删
2. **改用官方终端** — 已退役壳终端栈（前置已验证：本机 web profile 装载 ui-sidebar-terminal /
   terminal-controller / terminal-bash / terminal-pwsh，dump-config exit 0）
3. **保留权限 chip** — 未动

## 改动清单

删除源文件 9 个（3391 行）：

- client：right-sidebar.tsx(818) / right-sidebar-style.ts(608) / terminal-dock.tsx(506) /
  pty-store.ts(406) / terminal-prefs.ts(149) / xterm-css.ts(3)
- host：terminal-pty-api.ts(325) / pty-manager.ts(494) / terminal-prefs.ts(82)

修改源文件 5 个：

- `src/index.ts`：删两处终端 import、`...makePtyRoutes()` 路由项、拆解时的 `disposeAllPty()`；
  并修既有类型错误 —— `installSection` 能力守卫改用能力形状的本地类型（无 `any`，
  运行期 `typeof` 守卫逐字节不变）。该错误此前**阻断 `npm run build`**。
- `src/client/index.ts`：删三处终端 import 与 Ctrl+J 终端块（48 行），注释改为退役说明
- `src/client/backgrounds.ts`：删给已退役右栏写的死 CSS（`#dsh-hub-right-sidebar-root .mg-rs-*`，24 行）
- `package.json`：移除 `@xterm/xterm`、`node-pty` 依赖
- `src-tauri/tauri.conf.json`：移除 `../node_modules/node-pty/**/*` 资源项

## 验证

| 项 | 结果 |
|---|---|
| tsc（host + client 双配置） | exit 0 |
| build:client | exit 0，portability 与 platform-contract 两道断言通过 |
| 残留引用扫描 | 0 |
| `tauri.conf.json` | 仍是合法 JSON |
| CDP：槽位错误 / 控制台错误 | `[]` / 0 |
| CDP：座位归属（壳 / 官方） | 8 / 0（未被官方接管） |
| CDP：终端 dock / 右栏 root | 均不再创建（Ctrl+J 探测） |
| CDP：权限 chip / 会话标签栏 | 保留 |
| 产物 `mg-rs-` 残留 | 0 |

体积：`lib/client.js` **990 KB → 281 KB（-71%）**。

## 回滚

- 源码与改前件：`G:/DSH/DH-dev/_archive/hub-feature-trim-20260923/pre-edit/`（13 文件，保留目录结构）
- 运行副本：`_up_/lib/{client.js,client.js.map,index.js}.bak-featuretrim-20260923`
- 恢复右侧栏另有三处精确步骤，见 `src/client/index.ts` 的退役注释

## 生效条件

- **client 半包刷新页面即生效**（已验证）
- **host 半包需重启 Hub**（PTY 路由的移除在宿主侧）
- 恢复依赖：`@xterm/xterm` 与 `node-pty` 已从 package.json 移除，回滚时需一并还回

## 遗留

- `node-pty` 仍留在 `node_modules`（未卸载，仅磁盘占用，不再打包进安装器）
- 官方终端位于右侧栏；如需底部 dock 形态，回滚路径见上