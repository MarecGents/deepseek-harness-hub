# 发布记录 — 0.0.2-rc.4（2026-08-22 16:05）

## 版本内容

- **鲸鱼娘桌面图标全链路修复**（踩坑 #59–62）：ICON_BIG 任务栏槽（tao 同款 CreateIcon）、
  规范多尺寸 .ico（Pillow 重生成）、desktop_icon_ico_path 双重前缀修复、FOLDERID_Desktop
  桌面路径、AUMID 属性持久化、托盘图标随选择/主题同步。
- **安装器自包含插件本体**（踩坑 #63，本次核心）：tauri.conf resources 增补
  `../package.json`、`../cordis.patch.yml`、`../lib/**/*`、`../assets/**/*`、`../bin/**/*`
  → `$INSTDIR\_up_` 完整插件包；node.rs junction 三级优先 `_up_` → npm 副本 → dev 仓库根；
  repo_root() 改"完整 `_up_` 优先"判定。**根治：安装器此前从 npm registry 拉旧版插件，
  导致安装包永远缺最新功能。**
- repo_root dev 态优先修复（target 残留 _up_ 误判）、lib 构建漂移同步、踩坑 #24 复发注记。

## 发布产物

| 通道 | 状态 | 详情 |
|---|---|---|
| NSIS 安装器 | ✅ 已产出 | `build/0.0.2-rc.4/DeepSeek Harness Hub_0.0.2-rc.4_x64-setup.exe`（6.8MB，SHA256 `7ad941d0c32208a2a81558df690a0b7ab2c3d8af13a6f4b3098c4864dbfff955`），自包含全量插件（staged `_up_` 含 whale-girl/conversation-rail client bundle 已核验） |
| git tag | ✅ 已推送 | `v0.0.2-rc.4`（9c7f23c）；main/dev-v2 已同步 |
| verify-release 门禁 | ✅ ALL PASS | P1–P4（含干净安装装配冒烟，rc.4 tgz） |
| npm 插件层 | ⏳ **待补发** | 发布机无 npm 官方源凭据（ENEEDAUTH）。待 `npm login` 后执行下述命令 |

## 待补发命令（npm login 后，在 main @ 9c7f23c）

```sh
npm publish --access public --tag rc --registry=https://registry.npmjs.org/
npm dist-tag add @marecgents/dsh-hub@0.0.2-rc.4 latest --registry=https://registry.npmjs.org/
# 校验（registry 直查，勿用 npm view）：
# (Invoke-RestMethod 'https://registry.npmjs.org/-/package/@marecgents/dsh-hub/dist-tags').latest  # 必须为 0.0.2-rc.4
```

> 注：修复后的 NSIS 安装器**不依赖** npm registry 版本（junction 优先 `$INSTDIR\_up_`
> 自带包），测试机装包验证可先行；npm 补发只影响插件独立分发通道与 dsh-hub-win 兜底副本。

## 真机验证清单（BUILD.md §5）

1. 卸载旧版 → 安装 `0.0.2-rc.4_x64-setup.exe` → 首启"启动中"→ 自动进 dsh UI。
2. 设置 → DSH HUB：应出现「桌面图标」网格（5 张鲸鱼娘）——**本次修复的可见性验证点**。
3. 切换鲸鱼娘：任务栏按钮 + 标题栏/Alt-Tab + 托盘 + 桌面/开始菜单快捷方式应全部即时切换
   （Explorer 图标缓存延迟时重启一次资源管理器）。
4. 会话列表：对话定位条（rail）应正常显示。
5. junction 核验（可选）：`~/.dsh/profiles/web/node_modules/@marecgents/dsh-hub` 应指向
   `C:\Program Files\DeepSeek Harness Hub\_up_`。
