# 任务日志 — 卸载提速 + 安装进度反馈 + 启动平滑过渡（0.0.2-rc.7）

- **日期**：2026-08-22 17:20 – 19:10
- **分支**：dev-v2（a108342→4c225b4）→ PR #31/#32 → main（082ca74/8365af8），rc.7 已发布
- **产出**：NSIS `500eed93…4c71`；npm latest=rc=0.0.2-rc.7；tag v0.0.2-rc.7

## 过程摘要

1. 三项诉求：卸载慢、安装→卸载全流程体验（大厂参考）、启动慢+加载过渡。
2. 实现：PREUNINSTALL 快速通道 / POSTINSTALL 异步 bootstrap+进度轮询（ps1 -LogPath +
   哨兵）/ AUMID 后台线程 + 占位页统一 Splash 视觉 + 导航前淡出。
3. **6 轮沙箱 E2E 实测**驱动的三次修正：
   a. 哨兵 Write-Output 不落日志 → 安装轮询跑满 6min（改 Write-Dep）；
   b. Exec 异步清理被卸载器生命周期拖住 → ExecShell（ShellExecute）OS 级脱离；
   c. 空目录残留（临时副本句柄）→ 3s 延迟重试。
4. 归因方法论：钩子内 cmd 时间戳标记 + 主进程 -PassThru 探测，识破
   Start-Process -Wait 的 job-object 测量假象（把异步子进程计入可见时长）。
5. 终验（shipping bits）：可见卸载 1.4s；异步清理零残留；安装进度实时滚动；
   门禁 ALL PASS；npm/NSIS/tag/回灌全部完成。

## 遗留

- 真机复验（发布记录清单 3 项）。
- token 本轮再次使用后已从 ~/.npmrc 移除；对话中已多次明文出现，务必撤销重建。
