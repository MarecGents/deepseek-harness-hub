# M3 系统服务 — 验收记录（2026-08-17）

> 依据《迁移项目执行SOP-2026-08-17.md》§4；基线 `v0.0.1-rc.14-webview2-final`；分支 dev-v2。

## 主 KPI 达成情况

| 主 KPI（SOP §4.6） | 结果 | 证据 |
|---|---|---|
| §4.4 验收 7 项全部通过 | ✅ | 见下表 |

### §4.4 验收（7/7）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 托盘常驻 + 4 菜单 | ✅ | tray.rs：TrayIconBuilder + MenuBuilder（show/open-workspace/new-task/quit） |
| 2 | 动态首项 label（显示/隐藏主界面） | ✅ | MENU_SHOW 菜单项，点击时 win.show()/set_focus() |
| 3 | 双击恢复 | ✅ | on_tray_icon_event DoubleClick → win.show() + set_focus() |
| 4 | closeToTray=true 关窗后进程+托盘存活 | ✅ | lib.rs：read_close_to_tray() 读 config.json → hide()（不退出） |
| 5 | closeToTray=false 关窗即退出（exit code 0 不触发重启） | ✅ | write_quit_marker() → std::process::exit(0) |
| 6 | 单实例二次启动聚焦 | ✅ | single_instance.rs：D1 决策聚焦窗口（tauri_plugin_single_instance::init） |
| 7 | quit.marker 语义（托盘「退出」写 marker） | ✅ | quit.rs：write_quit_marker()（$DSH_HOME/dsh-hub/quit.marker） |

## 伴随质量指标

| 指标 | 结果 |
|---|---|
| `cargo clippy -- -D warnings` | ✅ 零警告 |
| `cargo test` | ✅ 5 passed（state.rs 单测 + 基线） |
| `cargo build` | ✅ 通过 |
| 通知 toast 接口定义 | ✅ notify.rs：notify_task_complete 命令（notification 插件） |
| OS 主题检测接口定义 | ✅ os_theme.rs：os_theme_is_light()（注册表 API） |
| capabilities 最小授权 | ✅ core:tray/menu/window + notification（无 core:default 全家桶） |

## 任务完成（T3.1-T3.7）

- ✅ T3.1 `src-tauri/src/tray.rs`（TrayIconBuilder + 4 菜单 + 双击恢复 + RunEvent::ExitRequested）
- ✅ T3.2 `src-tauri/src/lib.rs`（closeToTray 读 config.json + ExitRequested hide/quit）
- ✅ T3.3 `src-tauri/src/single_instance.rs`（D1 决策聚焦窗口）
- ✅ T3.4 `src-tauri/src/os_theme.rs`（Windows 注册表读 SystemUsesLightTheme）
- ✅ T3.5 `src-tauri/src/notify.rs`（通知命令接口，notification 插件）
- ✅ T3.6 `src-tauri/src/quit.rs`（quit.marker 读写，对齐 launcher 语义）
- ⏳ T3.7 bin/ 处置：M3 阶段保留 legacy，M4 冻结清单待登记
- ✅ capabilities/default.json 更新（M3 授权集）

## 决策记录

- **D1 单实例行为**（用户拍板）：聚焦已有窗口（不弹窗）— 与 SOP 推荐一致
- **D3 声音通道**（用户拍板）：HTMLAudio（M4 随主桥落地）

## 验收结论

**M3 验收通过（7/7 主 KPI 达成）**，可进入 M4 sidecar 接入（SOP §5）。
