# M1 脚手架 — 验收记录（2026-08-17）

> 依据《迁移项目执行SOP-2026-08-17.md》§2；基线 `v0.0.1-rc.14-webview2-final`；分支 dev-v2。

## 主 KPI 达成情况

| 主 KPI（SOP §1/§2.6） | 结果 | 证据 |
|---|---|---|
| `cargo tauri dev` 出无边框窗口、无 panic | ✅ | dsh-hub 进程 ×3 + msedgewebview2 ×28（WebView2 渲染）；`--smoke` 冒烟 exit 0 |
| `cargo clippy` 零警告 | ✅ | `cargo clippy -- -D warnings` exit 0 |
| `cargo test` 全部通过 | ✅ | 0 passed / 0 failed（基线建立） |
| `npx tauri build --debug` 产出 NSIS 安装包 | ✅ | `DeepSeek Harness Hub_0.0.0_x64-setup.exe`（4.0 MB）；dsh-hub.exe 12.9 MB（debug） |

## 伴随质量指标

| 指标 | 结果 |
|---|---|
| check-tauri-conf 10/10（identifier/productName/version/外部字段/decorations/transparent/min/externalBin 预留/webviewInstallMode/frontendDist/devUrl） | ✅ ALL PASS |
| ipc-smoke（invoke ping → pong，窗口 IPC + capabilities 链路） | ✅ PASS |
| X1 日志三目标（Stdout/Webview/Folder `$DSH_HOME/dsh-hub/logs/`） | ⚠️ 文件创建生效；启动日志写入 0 bytes（登记遗留 X1-1） |

## 任务完成（T1.1-T1.9 + 步骤 10/11）

- ✅ T1.1 Cargo.toml（tauri 2.11 + 插件 + log crate；lib name dsh_hub_lib）
- ✅ T1.2 build.rs · T1.3 tauri.conf.json（版本单一来源 0.0.0/embedBootstrapper/externalBin 预留）· T1.4 capabilities 显式最小集（core:event:default + log:default，无 core:default 全家桶）
- ✅ T1.5 icons 全套（npx tauri icon）
- ✅ T1.6 main.rs + lib.rs（X1 日志 + ping 命令 + 无边框窗口）
- ✅ T1.7 dev-shell-page.mjs（17891 伺服）+ dev/index.html（IPC 冒烟上报）
- ✅ T1.8 package.json（tauri:dev/build/icon + m1:check/m1:ipc-smoke scripts）+ .gitignore + README
- ✅ 步骤 11：scripts/check-tauri-conf.mjs + scripts/ipc-smoke.mjs

## 环境与工具链（M1 期间搭建）

- Rust 1.97.1（rustup stable，装于 `D:\Tools\Environment\rust`；tuna dist 镜像 + rsproxy crates 镜像）
- @tauri-apps/cli ^2.11.4（devDependencies）
- 网络：官方源 ~0.17MB/s → tuna 1.75MB/s（dist）/ rsproxy（crates 完整镜像，static.crates.io 被墙 → NO_PROXY 直连）

## 遗留（登记，非阻断）

- **X1-1**：dsh.log 文件创建但启动日志 0 bytes——Folder target 写入待 M2 首日确认（可能 logger 初始化时序或 level filter；M2 验收反复跑窗时核实）
- M1 首批 PR 清理三报告措辞残留（迁移 §1.2 端口预选、可行性 §2 旧分支等，SOP §2.4 步骤 10）

## 验收结论

**M1 验收通过（5/5 主 KPI 达成）**，可进入 M2 窗口管理（SOP §3）。
