# Phase 3 审查：SPT 分层收口（2026-08-18）

> 审查方式：静态代码链路推演，不运行实例。

## 结论

host 与 Rust 两侧均已按 SPT 职责分层，入口更薄，服务/控制器/模型职责清晰。

## 新增/调整

- `src/models/plugin-config.ts`
- `src/services/config-store.ts`
- `src/services/pins-store.ts`
- `src/controllers/shell-runtime.ts`
- `src/index.ts` 引入 controllers/services 能力
- 各层 AGENTS.md、README 同步

## 分层红线

- host：`index → controllers → core → managers/server/services → helpers → models/utils`
- Rust：`lib.rs → commands → managers/services → helpers`
- 未发现反向依赖。

通过。
