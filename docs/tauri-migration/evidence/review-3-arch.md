# review-3-arch — 架构最终验收（审查轮 3 / 3）

> 审查对象：《可行性分析报告》《代码迁移方案报告》《代码落地方案报告》（均 2026-08-17，docs/tauri-migration/，含「审查轮 1/2 修订记录」附录）
> 审查基线：`.planning/review-1-arch.md`（H1–H3/M1–M17/L1–L5）、`.planning/review-2-arch.md`（N1–N16 + 残留评估）、`.planning/research-A.md`、`REFERENCE.md`
> 审查角度：三轮闭环总验 / 三报告交叉一致性 / 架构可落地性 / 最终验收结论
> 结论：**可验收（架构维度）**。H1/H2/H3 与 N1–N5 全部闭环，N6–N16 及 R/G 系列修订与正文一致；三报告目标架构/进程模型/桥接/任务编号无实质残留矛盾；M1 脚手架任务清晰、依赖明确、验收可执行，无架构级遗留阻塞。残留 6 项均为措辞/记录级，建议 M1 首批 PR 顺手清理，不设验收门禁。

---

## 一、三轮闭环总验

### 1.1 轮 1 高严重度（H1–H3）—— 全部闭环

| 意见 | 闭环判定 | 正文落点（逐项核对） | 修订记录一致性 |
|---|---|---|---|
| **H1 桥选型三报告不一致** | ✅ 闭环 | 主桥 = HTTP/SSE/WS（页面↔sidecar，**服务端由 sidecar webserver 伺服**）、Tauri IPC 仅壳级本地命令、sidecar↔壳 stdio JSON-RPC：可行性 §3.4（L97-103）/P1（L203，含否决方案 A 理由）、迁移 §2.1（L57-63）/§3.1 index.ts 行（L94）/§8 M4（L268）、落地 §5.1（L152）/T4.5（L169）/T4.8（L172）/T4.9（L173）。三报告各执一词问题消除 | 可行性 L237 / 迁移 L318 / 落地 L350 均 ✅ |
| **H2 dangerousRemoteDomainIpcAccess 缺失/死结** | ✅ 闭环 | 「默认不配置、仅方案 A 需要」三报告统一（可行性 §3.4/§5.6、迁移 §2.1/§5.2、落地 T1.3 L47）；`__TAURI_INTERNALS__` 探测仅判「本地窗口 vs 远程页」、自有探针 `__DSH_SHELL__` 仅作客户端提示、接线信号改 env `DSH_HUB_SHELL=tauri`（可行性 §3.4 L103、迁移 §5.2 L196、落地 T4.6 L170）；M1 验收加本地窗口 IPC 冒烟（落地 §2.3-1 L57）；轮 2 残留的 dsh:output IPC emit 依赖已消除（见 N3） | 可行性 L238 / 迁移 L319 / 落地 L351 均 ✅ |
| **H3 sidecar↔壳命令通道无任务/notify 跨进程未定义** | ✅ 闭环 | stdio JSON-RPC 协议完整：可行性 §3.4（L102）、迁移 §4-9（L159）、落地 T4.1（L165）；`notifyTaskComplete`（sidecar session/event）→ stdio 上行 → `notify.rs` 弹 toast 跨进程路径闭环（落地 T3.5 L132/T4.6 L170）；方向标签经 N2 修正后全报告一致（见下） | 可行性 L239 / 迁移 L320 / 落地 L352 均 ✅ |

### 1.2 轮 2 高/中严重度（N1–N5）—— 全部闭环

| 意见 | 闭环判定 | 正文落点（逐项核对） | 修订记录一致性 |
|---|---|---|---|
| **N1 主桥服务端无任务且归属三方矛盾（高）** | ✅ 闭环 | **新增落地 T4.9 主桥服务端任务**（sidecar webserver 侧：注册/SSE/WS 升级/mg:* 路由/token 校验/dsh:output 下行/就绪握手，L173）；「index.ts 全原样」放宽为「插件层 API 原样 + 桥端点同层扩展」（迁移 §3.1 L94）；token = Rust 生成 → stdio/env 下发 → **端点侧（Node）校验**（可行性 §3.4/§5.6/P1、落地 X4-④ L227）；页面分发 = 初始化脚本注入 `window.__DSH_BRIDGE_TOKEN__`（禁 URL query）；依赖图 §8 更新 | 可行性 L271 / 迁移 L370 / 落地 L412 均 ✅ |
| **N2 stdio 方向标签矛盾（中）** | ✅ 闭环 | 全报告统一：stdout 侧（sidecar→壳）上行请求行 `notify/applyTheme/applySize` + 事件行 `READY/output/exit`；stdin 侧（壳→sidecar）下行命令仅 `quit`——可行性 §3.4（L102）、迁移 §2.1（L60）/§4-9（L159）/§5.2（L198）、落地 T4.1（L165）/T3.5（L132）/T4.6（L170）措辞逐字一致 | 可行性 L272 / 迁移 L371 / 落地 L413 均 ✅ |
| **N3 dsh:output/日志仍走 Tauri IPC emit（中）** | ✅ 闭环 | T4.1 stderr 不再 `app.emit("dsh:output")` → 经主桥 SSE/WS 下行（落地 T4.1 L165/T4.5 L169/T4.9 L173）；X1 Webview 目标标注「仅本地窗口/壳内页生效」（L224）；M4 验收「kill sidecar → 远程页看到 dsh:output 流」（§5.4-10 L186）；迁移 §2.1/§4-9、可行性 §3.4 同步 | 可行性 L273 / 迁移 L372 / 落地 L414 均 ✅ |
| **N4 T4.1/T4.3 进程拓扑重复（中）** | ✅ 闭环 | 收敛：T4.1 = 进程宿主 + stdio JSON-RPC 属主（Rust 直接 spawn dsh）；T4.3 降级为「仅 M5 externalBin/离线场景的 node 解析 + 装配辅助脚本，不承担进程宿主」（落地 T4.3 L167）；§8 依赖图（L253-255）同步 | 迁移 L373 / 落地 L415 均 ✅（可行性无正文改动点，缺行合理） |
| **N5 探测判据与握手二义（中）** | ✅ 闭环 | 两级判据 + env 接线信号 + 页面就绪握手全覆盖：可行性 §3.4（L103）、迁移 §2.1/§5.2（L196）、落地 T4.4（L168）/T4.6（L170）/T4.7（L171）/T4.9（L173）；验收 §5.4-12（L188） | 可行性 L274 / 迁移 L374 / 落地 L416 均 ✅ |

### 1.3 轮 2 低严重度（N6–N16）—— 全部闭环

| 意见 | 闭环判定 | 落点 |
|---|---|---|
| N6 L2 假闭环（icons/png-decode 无任务） | ✅ | 落地**新增 T2.6**（L91，M2 决策、M3 前落地）；迁移 §3.1 两行改指向 T2.6（L109-110） |
| N7 T3.2 依赖 T4.5 读 closeToTray | ✅ | T3.2：M3 阶段 Rust 直读 `$DSH_HOME/dsh-hub/config.json`（serde），M4 起可切换桥读取（L129） |
| N8 M16 深链启用路径无任务 | ✅ | §9 行 25：M5 决策启用 → T5.1 补 schemes 配置 + 注册插件 + 意图分发实现；延后 → 显式写「v1 不做」（L304） |
| N9 可行性 #18 未随 splash 收敛 | ✅ | 可行性 §2 #18 改官方双窗（L57）；修订表补 M5 行（L240） |
| N10 「透明窗口」措辞残留 | ✅ | 落地 §1.2 M1 行/§2.1 目标/§5.4-1 全部「无边框窗口（transparent:false）」（L23/L38/L177） |
| N11 invoke("bridge_message") 未入命令集 | ✅ | 迁移 §2.1 blockquote/§5.1 删除 bridge_message；命令集统一为 T4.8（set_window_theme/set_window_size/get_workspace_path）（迁移 L63/L181、落地 T4.8 L172） |
| N12 M12 口径算术 | ✅ | 可行性 §2 表尾可复核推导：21 = 16 项展开 18 行 + 3 新增（L65-68） |
| N13 dev/prod IPC 注入分叉 | ✅ | T4.4：dev 也坚持 `WebviewUrl::External` + 主桥（L168）；验收 §5.4-11（L187） |
| N14 T4.5 适配器交付方式 | ✅ | T4.5：Rust `initialization_script` 注入编译后适配器 + 探针，client bundle 与 tsdown externals 不动，与回退线注入共存（L169） |
| N15 T4.8 shell_command 形态 | ✅ | shell_command 不作为 invoke 暴露——托盘事件 Rust 侧触发 → 主桥 SSE/WS 下行（远程页）/ emit（本地窗口）（L172）；§9 行 14 同步（L293） |
| N16 通道 1 上下行清单矛盾 | ✅ | 迁移 §2.1 下行只保留 shell-command + 主题请求、上行 = workspace-path/session-focus/theme（L59）；落地 T4.5 下行清单删「工作区路径回执」（L169）；可行性 §3.4 同步（L101） |

### 1.4 R/G 系列（轮 2 risk/ops 意见）—— 抽查闭合，无残留

逐项核对正文：R1（随机端口双通道，可行性 #7 L46/迁移 §4-5/落地 T4.2）、R2（会话级 token + 端点校验 + WS/SSE Origin，可行性 §5.6/P1、落地 X4-④）、R3（stdio 帧前缀 + schema 校验 + READY「先验证再导航」+ exit 以 `Child::try_wait` 为准，迁移 §4-9/落地 T4.1/T4.4/T4.7）、R4（探针可覆写 → env 信号）、R5（`DSH_HOME` trim 后非空 + 单测，落地 T2.2）、R6（深链参数规范产出物，X4-⑥）、R7（卸载后 bundles/junction 清理 + `dsh web` 可启断言，T5.1/T5.5-⑥）、R8（SHASUMS256.txt.sig + expected-hash 登记，可行性 P2/落地 T5.2）、R9（webview-data 卸载清单，T5.1 ②）、R10（枚举自排除 → PID→端口解析 → 逐端口探活 + TOCTOU 二次确认，落地 T4.2/迁移 §4-5）、R11（B/C 通道 dsh 版本钉死 + 「SESSION_FORMAT_VERSION=0 无兼容承诺」显式引用 + 快照落点，落地 T5.3/§9 行 26/§10-7）、R12（私钥备份/双人审批/保管人 + 兼容期验证入 P4，T5.4）、R13（NSIS 升级检测旧版本 + 升级前杀进程，T5.1）；G1（`bundle.resources` 打包 + `DSH_HUB_PACKAGE_ROOT` 注入，T5.1/T4.2/T4.3/T5.5-②）、G2（当前包根重定义 + relink 判定，T4.2/迁移 §7.5-①）、G3（rc.14 过渡演练入 M5 验收，§6.3-5/迁移 §8）、G4（`--assemble-only`/`--smoke` 实现 = T4.4，T5.5-② 消费方，可行性 P2 注明等价）、G5（会话快照落点 `backup/v<from>/sessions-snapshot/` + 同挂所有升级入口，T5.3/X3/§9 行 26）、G6（卸载先杀主进程 + 超时强杀 + 运行中卸载用例，T5.1/T5.5-⑥）。

**闭环总验结论**：轮 1 H1–H3 与轮 2 N1–N5 共 8 项高/中严重度意见**全部闭环**，N6–N16 及 R/G 系列全部有正文落点且修订记录与正文一致；轮 2 点名的三个部分闭环项（L2→N6、M16→N8、M5→N9）均已补任务/补同步。修订记录缺行情况仅可行性轮 2 表缺 N16 一行（正文 L101 已内嵌 N16 标注并同步，见残留 #3），属记录完整性而非内容缺失。

---

## 二、交叉一致性核验（三报告对齐）

### 2.1 目标架构 / 进程模型 / 桥接 —— 完全对齐

| 收敛点 | 可行性 | 迁移 | 落地 |
|---|---|---|---|
| 三层进程模型（Rust 壳 / Node sidecar / webview 零改动） | §3.1（L83） | §1.1（L13-19）/§2.1（L41-55） | §1.2 M4（L26）/§5.1（L152） |
| 主桥 HTTP/SSE/WS，服务端 = sidecar webserver（T4.9），Rust 壳不实现 HTTP 桥 | §3.4（L101）/P1（L203） | §2.1（L59-63）/§3.1（L94） | T4.9（L173）/T4.5（L169） |
| stdio JSON-RPC 方向（notify/applyTheme/applySize 上行；quit 下行；READY/output/exit 事件行） | §3.4（L102） | §2.1/§4-9/§5.2 | T4.1/T3.5/T4.6 |
| Tauri IPC 仅壳级本地命令，命令集 = set_window_theme/set_window_size/get_workspace_path | §3.4（L103） | §2.1/§5.1（L181） | T4.8（L172） |
| 桥消息 mg:* 4+1 + 3 页面全局 + 上下行清单（workspace-path 只列上行） | §3.4（L101） | §5.2（L194-196）/§2.1（L59） | T4.5/T4.7（L171） |
| env 接线：DSH_HUB_LAUNCHED=1 / DSH_HUB_SHELL=tauri / DSH_HOME 显式传递 / DSH_HUB_PACKAGE_ROOT | §3.4（L103）/§5.2 | §4-9（L159）/§5.2（L196）/§7.5-① | T4.1（L165）/T4.6（L170）/T4.2/T4.3 |
| token 生命周期（Rust 生成 → stdio/env 下发 → Node 端点校验 → `window.__DSH_BRIDGE_TOKEN__` 注入 → 日志脱敏） | §3.4/§5.6/P1 | §2.1 | T4.5/T4.9/X4-④/X1 |
| splash 官方双窗（visible:false 主窗 + splash 窗 + close_splashscreen）+ M4 后资源来源 | §2 #18（L57） | §4-1（L135） | T4.4（L168）/§9 行 23（L302） |
| transparent:false（与 rc.14 一致，按需开启 + #32 像素验证） | §2 #2（L41） | §8 M1 行（L265） | T1.3/T1.6/§2.3-1/§5.4-1 |
| 装配归属 = sidecar（assemble-profile.mjs，T4.2），Rust 重写仅未来备选 | §2 #21/§3.3（L94） | §3.1/§6（L208） | T4.2（L166） |
| 多实例双通道（进程枚举 + 端口探活、不依赖固定端口、默认拒共存） | §2 #7（L46） | §4-5/§4-8（L147/L156） | T4.2/T4.4（L166/L168）/§5.4-8（L184） |
| dsh:output 经主桥 SSE/WS 下行（不依赖 IPC emit） | §3.4（L102） | §2.1/§4-9 | T4.1/T4.5/T4.9/X1 |

### 2.2 任务编号（T.x.x）交叉引用 —— 无悬空引用

- 落地定义全集：T1.1–T1.9、T2.1–T2.6、T3.1–T3.7、T4.1–T4.9、T5.1–T5.7、X1–X4。
- 可行性引用：T4.9（P1）、T4.2/T4.4/T4.1（P2）、T5.1/T5.5（G1/A6）、T4.6（N5）、T4.4/T4.1（G4/R6，见残留 #4）——均存在。
- 迁移引用：T4.1–T4.9、T2.2/T2.6、T3.3/T3.5/T3.7、T1.1/T1.3/T1.7/T1.8、T5.1–T5.7、X1–X4、§6.3-5/§3.4/§4.4/§5.4/§9/§10——均存在且章节号与落地实际一致。
- 里程碑/验收互指：迁移 §8 M2→「落地 §3.4 对比表」、M3→「落地 §4.4」、M4→「落地 §5.4」、M5→「落地 §6.3」——落地对应章节内容吻合。

### 2.3 口径统一核对

- 26 文件权威口径（17 host + 7 launcher + 2 快捷方式）：迁移 §3（L88）、可行性 §2 表尾（L69）、research-A 24 旧口径注明——一致。
- 21 能力项口径（12 直接覆盖 + 5 移植 + 3 保留 + 1 待验证）：可行性 §2 合计行（L61）与 §7.1（L195）复算一致；16/18/21 推导可复核（L65-68）。
- 体积双口径（壳二进制 ~10MB 级 / 安装包 40-80MB 级）：可行性 §5.1/§7.1/P2、迁移 §7.1、落地 §6.3-4/§10-6——一致。
- 版本单一来源（package.json ↔ tauri.conf version ↔ Cargo 恒 0.0.0）：迁移 §7.4（L244）、落地 T1.1/T1.3（L45/L47）——一致。
- 决策点清单：落地 §10-5（①单实例聚焦 ②托盘隐藏时语义 ③端口策略已收敛 ④get_workspace_path 方向）+ T4.8 M4 前截止；迁移 §9.2 待验证 11 项 + 可行性 §7.2 P1–P8——三报告互为锚定，无缺项（残留 #1 的「端口预选」表述见下）。

**交叉一致性结论**：目标架构、进程模型、桥接三通道、任务编号、口径数字在修订后**完全对齐，无实质残留矛盾**。

---

## 三、可落地性评估（架构维度）

1. **M1 脚手架可直接开工**：T1.1–T1.9 文件级任务（Cargo.toml/build.rs/tauri.conf/capabilities/icons/main+lib/dev 页/package.json），依赖「无」（落地 §1.2），验收 5 条可执行（`cargo tauri dev` 出无边框窗、字段核对、`tauri build --debug` NSIS、clippy/test、transparent 按需像素验证）；M1 骨架字段一次到位（identifier/productName/version 单一来源/externalBin 预留/embedBootstrapper）。依赖图 §8 给出 M1→M2→M3→M4→M5 与并行支线（T4.2 第一天即可开工、M2 三文件/M3 四模块并行）。
2. **前置条件不阻塞架构开工**：P1–P8 验收标志明确、状态「待办」属组织/验证类前置；P6（M1/M2 最小闭环）即开工本身；P4/P5（插件 API、tao/wry 平台行为）有明确核实路径（clone plugins-workspace / 最小实验窗口），且 M1 不依赖。
3. **决策点均有截止与默认**：单实例聚焦（需用户确认，默认聚焦）、get_workspace_path 应答方向（M4 前截止，候选列明）、声音三选一（M3/M4 前闭合，候选列明）、PATH 命令与壳自崩溃守护与深链（M5 决策，均有「显式声明/延后」出口）——无悬空决策导致架构无法推进。
4. **待验证项清单完整**（迁移 §9.2 11 项 + 落地 §10 10 项 + 可行性 P4/P5），全部标注「动工前确认」且集中在插件 API/平台行为，非架构不确定性。
5. **架构级遗留阻塞：无**。桥服务端归属（T4.9）、stdio 方向、进程拓扑、探测握手、token 生命周期五个轮 2 焦点全部有明确实现任务与验收。

---

## 四、残留问题清单（非阻塞，建议 M1 首批 PR 顺手清理）

以下 6 项均为**措辞/记录级**，不影响架构成立与开工，不设验收门禁：

1. **迁移 §1.2（L24）「行为变更（单实例二次启动聚焦、端口预选）」表述过时**：端口策略已收敛为「保留 rc.14 `--port 0` 随机端口、取消 portpicker 固定端口」（迁移 §4-8、落地 §10-5 ③），不再是行为变更决策点。建议删「端口预选」，仅保留单实例聚焦。
2. **可行性 §2 #21（L60）行内残留「或 Rust 首启调用一次」旧分支**：修订 M7 已收敛为 sidecar 装配（§3.3/迁移 §6/落地 T4.2），建议删旧分支以免读者误解双归属。
3. **可行性轮 2 修订记录表（L269-285）缺 N16 行**：正文 §3.4（L101）已内嵌「审查轮 2 修订 N16」标注并同步，仅修订表缺行，建议补一行。
4. **可行性 P2（L204）「Rust 实现任务见落地 T4.4/T4.1」引用略宽**：`--assemble-only`/`--smoke` 的实现点在 T4.4（落地 G4 修订明确），T4.1 为 M4 启动链路上下文；建议改「T4.4（装配依赖 T4.2）」。
5. **落地 T2.1（L86）「monitor API 形态待验证」与可行性 §2 #17（L56）「已核实」证据状态措辞差异**：T2.1 实际待验证的是 scale_factor 换算**行为**（§9 行 2 已注明「M2 首日最小实验验证」），API 存在性已核实；建议措辞对齐为「API 已核实、单位语义行为待验证」。
6. **（可选）Node 体积区间表述**：迁移 §9.2-6「Node 24 压缩后 30MB+」vs 可行性 §5.1「25-40MB/平台」——区间兼容（30+ ⊂ 25-40），不矛盾；若求严谨可统一为「25-40MB」。

---

## 五、最终验收结论

**架构维度：可验收。**

- 三轮修订闭环成立：轮 1 H1/H2/H3、轮 2 N1–N5 全部闭环，N6–N16 与 R/G 系列均有正文落点，修订记录与正文一致（仅 2 处记录/措辞级瑕疵，见残留 #3/#4）。
- 三报告交叉一致：目标架构（三层进程模型）、桥接（主桥 HTTP/SSE/WS + sidecar 伺服 T4.9 / stdio JSON-RPC 方向 / 壳级 IPC 三命令集）、任务编号（T1.x–T5.x/X1–X4 无悬空引用）、口径（26 文件/21 能力项/体积双口径/版本单一来源）完全对齐。
- 可落地：M1 脚手架任务、依赖、验收全部可执行；前置条件与决策点均不阻塞开工；无架构级遗留阻塞。

**验收条件（不设门禁）**：建议在 M1 首批 PR 中顺手清理第四节 6 项措辞/记录级残留，其中 #1/#2 属旧分支表述、#3/#4 属记录完整性、#5/#6 属措辞统一——均不影响「可无缝迁移落地」的架构判定。可以进入 dev-v2 的 M1 里程碑实施。

---

## 附：审查对照速查（轮 3）

| 审查角度 | 结论 |
|---|---|
| 轮 1 闭环（H1/H2/H3） | ✅ 全部闭环，正文+修订记录双验证 |
| 轮 2 闭环（N1–N5） | ✅ 全部闭环（N1 补 T4.9；N2/N3/N4/N5 收敛到位） |
| 轮 2 低（N6–N16）/ R / G 系列 | ✅ 全部有落点；部分闭环项（L2/M16/M5）已补任务/同步 |
| 三报告交叉一致性 | ✅ 无实质残留矛盾（残留 #1/#2 为旧分支措辞） |
| 任务编号 T.x.x | ✅ 无悬空引用（残留 #4 为宽引用非错误） |
| 可落地性（M1） | ✅ 任务/依赖/验收可执行；前置与决策点不阻塞 |
| 架构级遗留阻塞 | 无 |
| 最终结论 | **可验收（架构维度）**，附 6 项非阻塞清理建议 |
