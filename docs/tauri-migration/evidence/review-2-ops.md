# 审查意见 2/3 — 兼容性与运维复核（dsh-hub Tauri 迁移三报告）

> 审查轮：2 / 3（兼容性与运维 · 修订复核）
> 审查员：兼容性与运维审查子代理（第二轮）
> 日期：2026-08-17
> 审查对象（修订后版本）：
> - `docs/tauri-migration/可行性分析报告-2026-08-17.md`（「可行性」）
> - `docs/tauri-migration/代码迁移方案报告-2026-08-17.md`（「迁移」）
> - `docs/tauri-migration/代码落地方案报告-2026-08-17.md`（「落地」）
> 第一轮意见：`.planning/review-1-ops.md`（OPS-H1~H5 / M1~M9 / L1~L6）
> 参照基线：`.planning/research-D.md`、`REFERENCE.md`
> 抽查实证：`src/services/state-store.ts`（L33-35 `dshHome()`）；`--assemble-only`/`--smoke`/`DSH_HUB_ASSEMBLE_ONLY` 在三报告的全部出现点。

**结论速览：修订质量高，第一轮 15 项意见中 13 项已闭环、2 项「声明闭环但任务层未收口」（OPS-H2 的 M5 过渡演练、OPS-M7 的 `--assemble-only` 实现任务），另发现 4 条新问题（其中 3 条中危，均属「补任务即闭环」型），总体维持「有条件通过」，不改变可行性「高」评级。**

---

## 1. 修订验证结论（逐项）

### 1.1 高严重度（第一轮 H1–H5）

| 意见 | 结论 | 验证依据 |
|---|---|---|
| **OPS-H1** $DSH_HOME 解析 | ✅ **已闭环** | 三报告统一：可行性 §5.4 路径/权限行 + P7「`DSH_HOME env \|\| ~/.dsh`（homedir），不用 `dirs::data_dir()`」；迁移 §4-10「与 `state-store.ts dshHome()` 逐字一致 + spawn 前显式 `env("DSH_HOME", 解析值)`」；落地 T1.1（`dirs` 仅用于 `home_dir()`）、T2.2（解析函数 + 单元测试入 M1/T2.2）、T4.1/T4.3（显式 env 传递）。抽查源码确认：`state-store.ts` L33-35 `dshHome() = env DSH_HOME(trim 非空) ?: homedir()/.dsh`，与报告表述一致。残留低危见 §4-R5（env 空白串处理）。 |
| **OPS-H2** 存量 npm→安装器过渡 | 🔶 **基本闭环（1 处未收口）** | 迁移新增 §7.5 四步齐全：① 旧 junction relink（新壳首启「目标检测 + relink」）② 快捷方式误删防护（改名或先卸旧包顺序 + postuninstall 不得触碰 `$DSH_HOME`/安装目录）③ 旧 PATH 命令并存（按 OPS-M5 决策 + 升级演练断言）④ 数据与升级演练；可行性 §5.2/P7 同步；落地 T5.6 固化。**未收口点**：§7.5-④ 声称「升级演练写入 M5 验收」，但落地 §6.3 验收清单（仅 4 条：三端产物/全新环境首启/更新闭环/体积口径）与迁移 §8 M5 行的验证列均**无 rc.14→安装器过渡演练条目**（见新问题 G3）；装配源打包与 relink 目标注入另见 G1/G2。 |
| **OPS-H3** 卸载清理语义 | ✅ **已闭环** | 落地 T5.1 自定义 NSIS 三段清理：必删（安装目录/快捷方式/开始菜单/Uninstall 键）→ 可选勾选 `$DSH_HOME/dsh-hub/` 自有文件 + `$DSH_HOME/dsh-hub-window-state.json`（默认不删）→ 默认保留 `$DSH_HOME/profiles/` 与 `sessions/`（或独立勾选+强警示）+ 卸载前杀 sidecar；T5.5-⑥ 卸载冒烟（装→卸→断言三段边界）；可行性 §5.2 同步。残留低危见 §4-R7（webview-data 未入清单）、G6（dsh-hub.exe 自身占用）。 |
| **OPS-H4** 声音跨平台 | ✅ **已闭环** | 三报告统一三选一（HTMLAudio / Rust rodio-cpal / 平台命令 afplay·paplay·aplay）且决策截止 M3/M4：可行性 #15/§5.4、迁移 §3.1、落地 T4.6/§10-8；§9 行 15 验收方法含「逐端列：macOS/Linux 声音通道各自验收」。残留低危见 §4-R4（T4.6 漏 aplay）。 |
| **OPS-H5** 三端逐端验收 | ✅ **已闭环** | §9 表 28 行全部带「平台」列；关键跨端差异行有平台标注（行 4 Linux appindicator 待实测、行 7 Windows、行 10 macOS/Linux 逐端像素验证、行 14 Windows 激活 vs macOS/Linux open 直开、行 24 Windows、行 28）；M5 §6.3-1 逐端清单（托盘/通知/标题栏 traffic lights + `--titlebar-offset`/声音/深链/无边框 WM/#32 像素验证以平台列纳入）。 |

### 1.2 中严重度（第一轮 M1–M9）

| 意见 | 结论 | 验证依据 |
|---|---|---|
| **OPS-M1** 桥接选型 | ✅ 已闭环 | 可行性 §3.4 / 迁移 §2.1 / 落地 T4.5-T4.8 统一主桥 = HTTP/SSE/WS、Tauri IPC 仅壳级本地命令、sidecar↔壳 stdio JSON-RPC；P1 改为选型冻结任务。 |
| **OPS-M2** AUMID | ✅ 已闭环 | §9 行 24（Windows：任务栏名称/图标/同组/快捷方式 AUMID == 应用 AUMID/toast 回窗）+ 迁移 §3.1 app-id 行 + §9.2-9「identifier→AUMID 最终形态列入 P5」+ 落地 T5.1 快捷方式 AUMID 一致。 |
| **OPS-M3** 启动屏 | ✅ 已闭环 | §9 行 23（冷启动无白/黑屏帧、splash 主题与 SPA 一致深/浅各一次、splash→SPA 无闪烁）+ T4.4 官方双窗（visible:false 主窗 + splash 窗 + close_splashscreen）+ 迁移 §4-1 资源来源定义。 |
| **OPS-M4** dsh.log 位置 | ✅ 已闭环 | 落地 X1「sidecar node 侧日志改写至 `$DSH_HOME/dsh-hub/logs/dsh.log`（安装目录可能只读）+ 与 tauri-plugin-log Folder target 并列」；§9 行 22 同步；resetLog「保留或改为按大小/天数轮转」；排障验收（模拟 node 缺失 → 断言两个日志目标都有内容）写入 §9 行 22。 |
| **OPS-M5** PATH 命令决策 | ✅ 已闭环（决策点） | 迁移 §3.2 dsh-hub.mjs 行 + §7.5-③、落地 T3.7/T5.1 三处口径统一：M5 显式决策「保留（安装器注册 PATH→`dsh-hub.exe`，开壳语义）或放弃（文档化替代 `dsh web` + 桌面快捷方式）」，升级演练断言旧命令行为一致。 |
| **OPS-M6** 体积口径 | ✅ 已闭环 | 可行性 §5.1/§7.1/P2、迁移 §7.1、落地 §6.3-4 统一「壳二进制 ~10MB 级 + 安装包 40-80MB 级」双口径，删除「安装包 ~10MB」硬验收，A/B/C 通道决策前置 M5。 |
| **OPS-M7** CI 冒烟可执行性 | 🔶 **规范层闭环 / 落地层未闭环** | 需求与逐端命令已齐且两报告一致：迁移 §7.3 P4 == 落地 T5.5-②/③（`--assemble-only`/`--smoke` 诊断参数等价 `DSH_HUB_ASSEMBLE_ONLY=1`；Windows NSIS `/S`；macOS `hdiutil attach` 或 `--smoke`；Linux `--appimage-extract`/xvfb + `--smoke`；空 `$DSH_HOME` 断言 scoped 装配）。**未闭环点**：整个落地任务清单（T1.6/T4.1/T4.4/T4.8/T5.x）没有一项实现该 CLI 参数（arg parse + 装配 + 验证窗口可创建 + 立即退出）——T5.5 是消费方，依赖它先存在（见新问题 G4）。 |
| **OPS-M8** WebView2 installMode | ✅ 已闭环 | 落地 T1.3 补 `webviewInstallMode: embedBootstrapper` + T5.5-③「Win10 无 WebView2 Runtime 隔离机 → 自动装 Runtime 并成功启动」用例；迁移 §7.1 同步。 |
| **OPS-M9** AGENTS.md 改写 | ✅ 已闭环 | 落地 T5.6 拆「改写稿（diff 草案）→ 评审 → 合入」三步，覆盖双门禁职责（npm verify-release vs 壳 verify-tauri-release）、铁律 8 改写（测试机装安装器首启冒烟）、install:local → `cargo tauri build --bundles nsis`、版本单一来源、postuninstall 不误删；迁移 §7.4 同步。 |

### 1.3 低严重度（第一轮 L1–L6）

| 意见 | 结论 | 验证依据 |
|---|---|---|
| OPS-L1 Cargo 版本矛盾 | ✅ 已闭环 | 落地 T1.1 `version="0.0.0" publish=false`，T1.3 `version: "../package.json"`。 |
| OPS-L2 X3 备份路径歧义 | ✅ 已闭环 | 落地 X3/T5.3 精确路径：window-state 在 `$DSH_HOME` 根；config/pins 在 `$DSH_HOME/dsh-hub/` 下。 |
| OPS-L3 深链范围 | ✅ 已闭环 | §9 行 25 显式「v1 范围外」+ M5 决策启用路径 + 输入校验（B3）前置。 |
| OPS-L4 崩溃现场信息 | ✅ 已闭环 | 落地 X2 crash.log 头记录「版本 + commit + OS + tauri/cargo 版本」。 |
| OPS-L5 §9 表状态列 | ✅ 已闭环 | §9 表末列「状态」（已核实/待验证/需用户确认/决策点）。 |
| OPS-L6 WebView2 数据目录 | ✅ 已闭环 | 落地 X4-⑦ 显式配置 `$DSH_HOME/dsh-hub/webview-data` + 备份/清理边界 + localStorage 核实。残留低危见 §4-R7。 |

---

## 2. 行为对齐完整性（§9 验收总表复核）

- **行数**：28 行（第一轮 22 行 + 新增 6 行：23 启动画面 / 24 进程身份 AUMID / 25 深链 / 26 会话兼容 / 27 壳自身崩溃守护 / 28 多显示器光标屏 3/4）。第一轮点名的 3 个缺失（启动屏/AUMID/深链）与 2 个部分覆盖（事件音跨平台、多显示器）全部补行。
- **能力覆盖**：可行性 §2 的 21 项能力逐项可在 §9 找到对应行（#1-3→行 1-3、#4→4、#5→6、#6/7→8/9、#8→17+27、#9→25、#10→19、#11→行 13/16/20 隐含（M4 验收 5.4-1 覆盖）、#12→14、#13→3/13、#14→15、#15→15、#16→14、#17→28、#18→23、#19→24、#20→18、#21→16），无遗漏。
- **平台列合理性**：跨端差异行均有平台标注；行 15（声音）与行 10/12（fixed 合成渲染）明确「逐端」验收。合理。
- **残留低危**：① 无边框 + 透明窗口无独立 §9 行（仅 M1 §2.3 验收点 1 与行 1 隐含，可接受）；② 平台列偶混验证状态字样（行 4/6/10/28 的「已核实/待验证」宜归入状态列，见 §4-R3）。

---

## 3. 新发现问题清单（本轮新引入/新发现）

| # | 严重度 | 描述 | 涉及章节 | 修改建议 |
|---|---|---|---|---|
| **G1** | 中 | **安装器「内置 dsh-hub 插件包」无资源打包任务**。可行性 §5.2 与迁移 §7.5-① 均以「安装目录内嵌资产」为装配源（relink 目标），但落地任务清单只有 T5.1（externalBin/node + NSIS 模板）、T5.2（node 下载）——没有任何任务把 dsh-hub 包本体（`lib/` + `assets/` + client bundle）声明为 `bundle.resources` 打进安装器，也没定义 assemble-profile.mjs 如何获知该资源路径。 | 可行性 §5.2；迁移 §7.5-①；落地 T5.1/T5.2/T4.2/T4.3 | T5.1 增 `bundle.resources` 声明（或等价资源布局）；T4.2/T4.3 定义装配源路径注入（如 `DSH_HUB_PACKAGE_ROOT` env 或相对 sidecar 脚本解析），并在 T5.5 冒烟断言装配源指向安装目录内嵌资产。 |
| **G2** | 低-中 | **junction「当前包根」在安装器时代的判定/relink 目标未定义**。迁移 §7.5-① 要求新壳首启自带「目标检测 + relink」，但落地 T4.2 内容要点只写「junction 自愈（→ 当前包根）」——「当前包根」在 Tauri 时代应重定义为安装器内嵌资产路径（而非 npm 全局包），该重定义与「目标非安装目录内嵌资产则重指」的判定分支未写进 T4.2。 | 迁移 §7.5-①；落地 T4.2 | T4.2 内容要点补一句：`当前包根` = 安装器内嵌 dsh-hub 资产路径（G1 注入方式）；junction 目标非该路径即 relink（与 rc.14 自愈语义等价），并保留 bare 遗留清理。 |
| **G3** | 中 | **rc.14→安装器过渡演练未入任何 M5 验收清单**。迁移 §7.5-④ 声称「升级演练写入 M5 验收」，但落地 §6.3 验收仅 4 条（三产物/全新环境首启/更新闭环/体积口径），迁移 §8 M5 行验证列也未列——「装有 rc.14 的隔离环境：装安装器 → 首启 → 数据仍在 → 旧命令行为一致」无处执行，OPS-H2 的收口因此不完整。 | 迁移 §7.5-④/§8 M5；落地 §6.3 | 落地 §6.3 补第 5 条「存量 rc.14 过渡演练」（含 `$DSH_HOME` 数据保留断言 + 旧 `dsh-hub` 命令行为断言），并在迁移 §8 M5 验证列同步。 |
| **G4** | 中 | **`--assemble-only`/`--smoke` 诊断参数无实现任务**。迁移 §7.3 P4 与落地 T5.5-② 定义了需求（等价 `DSH_HUB_ASSEMBLE_ONLY=1`：只装配 + 验证窗口可创建后立即退出），T5.5 是消费方，但 Rust 侧没有任何任务（T1.6/T4.1/T4.4/T4.8/T5.x）实现该 CLI 参数解析与「装配-即退」流程——门禁脚本会在不存在该参数时直接失败，CI 冒烟可执行性仍停留在纸面。 | 迁移 §7.3 P4；落地 T5.5-②/§9 行 20 | 在 T4.4（lib.rs 启动编排）或 T4.1 增子任务：解析 `--assemble-only`/`--smoke` → 调用装配（T4.2）→ 验证窗口可创建 → exit 0；注明与 `DSH_HUB_ASSEMBLE_ONLY=1` env 等价；可行性 P2 的 `DSH_HUB_ASSEMBLE_ONLY=1` 表述同步（见 §4-R6）。 |
| **G5** | 低 | **会话只读快照的存放位置与触发入口未定义**。落地 T5.3/X3 只写「dsh 大版本升级前对 `$DSH_HOME/sessions/` 做只读快照」，未定义快照落点、是否随 config 备份进 `backup/v<from>/`、由哪个升级入口（updater / NSIS 升级脚本）触发。 | 落地 T5.3/X3；§9 行 26 | 补「快照至 `$DSH_HOME/dsh-hub/backup/v<from>/sessions-snapshot/`（只读复制或硬链）+ 与 config 备份同挂所有升级入口」；T5.5-④ 演练加断言。 |
| **G6** | 低 | **卸载必删安装目录时 dsh-hub.exe 自身占用未处理**。T5.1 只写「卸载前杀 sidecar」，主进程（Tauri exe）占用安装目录会导致必删项删除失败。 | 落地 T5.1 | NSIS 卸载模板补「结束 dsh-hub.exe 主进程（或弹「请先退出应用」）+ 超时强杀」；T5.5-⑥ 卸载冒烟加「运行中卸载」用例。 |

---

## 4. 残留问题清单（第一轮低中危处理的合理性 + 新发现的低危）

| # | 严重度 | 描述 | 结论/建议 |
|---|---|---|---|
| R1 | 低 | **research-D.md 基线文本未同步**（§4 表「$DSH_HOME 语义」行仍写「默认 `dirs::data_dir()`（可保持 ~/.dsh 兼容）」自相矛盾原文；「卸载」行仍写 `$APPDATA/<bundleId>`；PATH 行断言「安装器默认不建」）。三报告均已正确偏离并显式注明（迁移 §4-10 解释了为什么不用 data_dir），但基线文档留旧文，后续实现者按 research-D 检索会误读。 | 建议 research-D 文件头加「本表 $DSH_HOME/卸载/PATH 三行已被三报告修订取代」注记或直接更新三行；不改报告本身（报告处理正确）。 |
| R2 | 低 | §9 无「无边框 + 透明窗口」独立验收行（透明像素验证在 M1 §2.3-5、无边框在 M1 §2.3-1）。 | 可接受（M1 验收已覆盖）；如要全表自洽可并入行 1 验收方法。 |
| R3 | 低 | §9 平台列偶混验证状态（行 4/6/10/28 含「已核实/待验证」字样），与末列「状态」信息重叠。 | 建议平台列只写平台范围，验证状态归状态列；不阻塞。 |
| R4 | 低 | 落地 T4.6 声音三选一漏写 `aplay`（写「afplay/paplay」，与 §10-8/可行性 #15 的「afplay/paplay/aplay」不一致）。 | 补 `aplay`。 |
| R5 | 低 | 「逐字一致」的 `dshHome()` 含 env 空白串处理（`state-store.ts` L35 `env.trim() !== ''`），迁移 §4-10/T2.2 写「非空」未提 trim。 | Rust 解析函数按 `trim 后非空` 实现 + 单测覆盖 `DSH_HOME=' '` 场景，以真「逐字一致」。 |
| R6 | 低 | 可行性 P2 仍写 `DSH_HUB_ASSEMBLE_ONLY=1` 装配诊断，未同步 `--assemble-only/--smoke`（迁移 §7.3/落地 T5.5 已改参数化）。 | 因「等价」关系已注明可接受；建议 P2 补一句「等价 Tauri 诊断参数 --assemble-only/--smoke」与 G4 联动。 |
| R7 | 低 | 卸载可选勾选清单（T5.1）未列 `webview-data`，与 X4-⑦「纳入清理边界」表述一致性存疑（若可选勾选删除整个 `$DSH_HOME/dsh-hub/` 则已覆盖，属表述歧义）。 | 建议 T5.1 可选勾选清单显式加 `webview-data`（或注明「随 `$DSH_HOME/dsh-hub/` 整目录」）。 |

---

## 5. 结论

1. **修订质量**：第一轮 15 项意见中 13 项完全闭环（H1/H3/H4/H5、M1–M6/M8/M9、L1–L6），§9 验收表扩至 28 行且平台列/状态列齐备，覆盖可行性 21 项能力与第一轮全部缺口；修订痕迹（附录「审查轮 1 修订记录」）与实际文本一致，未发现编造或红线削弱。
2. **两处「声明闭环、任务层未收口」**：OPS-H2（§7.5-④ 声称的 rc.14 过渡演练未进落地 §6.3 M5 验收，G3）与 OPS-M7（`--assemble-only/--smoke` 只有需求与消费方、无 Rust 实现任务，G4）——建议在 M1 动工前把这两条补进落地任务清单。
3. **三条中危新问题需补任务**：G1（安装器内置 dsh-hub 插件包的资源打包与装配源注入）、G3、G4；其余为低危表述/一致性微调（G2/G5/G6、R1–R7），可在开发中顺手修正。
4. **总体评级维持「有条件通过」**：剩余缺口均为「补任务/补验收条目即闭环」型工程问题，不改变迁移可行性「高」评级；建议修订完成后再进入第三轮审查（实施/工程可执行性视角）。
