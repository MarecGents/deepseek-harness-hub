# dsh-hub Tauri 2.x 迁移三报告 — 风险与安全最终验收报告（审查轮 3/3）

> 审查对象：《可行性分析报告》《代码迁移方案报告》《代码落地方案报告》（2026-08-17，均含「审查轮 1/2 修订记录」附录）
> 审查角度：三轮闭环总验 / 安全设计可执行闭环 / 残余风险显式记录 / 三报告一致性终核 / 最终验收结论
> 依据：三报告全文（含附录）+ `.planning/review-1-risk.md`（轮 1）+ `.planning/review-2-risk.md`（轮 2）+ `.planning/research-B.md` / `.planning/research-C.md`
> 纪律：仅基于实际读到的内容；证据列「报告 · 节 / 行号」
> 严重度沿用：【S】阻断 ·【H】高 ·【M】中 ·【L】低

---

## 〇、结论速览

**可验收（有条件通过）**：轮 1 的 1 个阻断项 + 7 个高危项、轮 2 的 2 个 M 级执行缺口（R2/R3）+ 1 个 M 级未评估影响（R7）及全部 L 级一致性/细节项（R1/R5/R6/R8–R13）**均已闭环且任务落位**；安全设计五大面（capabilities 最小授权 / token 生命周期 / stdio 帧标识 / 卸载清理 / 供应链）**全部形成「起步 → 执行 → 验收/门禁」可执行闭环**；残余风险均为**显式记录的决策点 / 前置条件 / 可接受差异**，无未记录或悬空项。剩余「待验证」项按报告自身排期（P4/P5 动工前、M3/M4/M5 前截止）闭合即可，不构成验收阻塞。

---

## 一、三轮闭环总验

### 1.1 轮 1 高危/阻断项（A1/A2/B1/B2/B3/C1/E1）

| 项 | 轮 1 严重度 | 轮 2 状态 | 轮 3 终验证据（本轮实际读到） | 结论 |
|---|---|---|---|---|
| A1 多实例双通道 | S | 迁移/落地闭环；可行性残留「3080」+修订表缺行 | 可行性 §2 #7（L46）已改「进程枚举（命令行匹配 `dsh.*web`）+ 端口探活双通道、**不依赖固定端口**（保留 rc.14 `--port 0` 随机端口，壳经 sidecar stdout READY/URL 行读端口），**删除『探测 3080 端口』固定端口旧设计残留**」，与迁移 §3.2/§4-5/§4-8、落地 T4.2/T4.4/T5.5-⑧ 文本一致；可行性「审查轮 1 修订记录」表已有 A1 行（L247，标注与迁移/落地一致） | ✅ 闭环 |
| A2 回退线探测 | H | 闭环 | 可行性 §3.4（L103）两级判据 + 自有探针 `__DSH_SHELL__` 降级为客户端提示；迁移 §5.2（L196-197）；落地 T4.5/T4.6（L169-170）接线信号改 env、远程页不依赖探测 | ✅ 闭环 |
| B1 capabilities 最小授权 | H | 任务闭环，残留 `core:default` | 落地 T1.4（L48）**显式最小集（不用 `core:default` 全家桶——自动聚合含 path 等多余面，已核实 tauri build.rs 聚合行为；起步不含 `shell:default`；按窗口 label 分组授权 main vs splash）** + X4-① 终审产物（最终清单+每权限注释+页面/命令/权限三元组）+ M4 用 `tauri dev` ACL 报错驱动「真实调用点 → 加对应权限」 | ✅ 闭环 |
| B2 桥鉴权 | H | 任务单列，校验方/token 生命周期错位 | 可行性 §5.6（L169）：「端点由 **sidecar（Node host half）伺服并校验**（Rust 壳不在 HTTP 路径上，实现者不得照旧文在 Rust 侧建校验）」+ Origin 校验（WS/SSE upgrade 与 POST 同强度；WS 无 CORS，Host 头对 IP 直连恒等于白名单值，Origin+token 才是真闸门）+ **会话级 token 全生命周期**（签发→下发→注入→轮换→日志脱敏）；落地 X4-④（L227）+ T4.9（L173）实现方落点 | ✅ 闭环 |
| B3 deep-link 输入校验 | M | 闭环，残留产出物未列 | 迁移 §4-7（L153）scheme/host 白名单 + URL 规范化 + 路径白名单 + ≤5MB + 安全设计落地前 M5 不启用；落地 X4-⑥（L227）**补产出物行：深链参数规范（白名单表+规范化规则+路径白名单+大小限制），M5 决策启用前评审** | ✅ 闭环 |
| C1 SessionEvent 兼容 | H | 基本闭环，残留快照语义/dsh 版本钉死 | 落地 T5.3（L204）只读快照**落点 `$DSH_HOME/dsh-hub/backup/v<from>/sessions-snapshot/` + 只读校验 + 保留策略 + 同挂所有升级入口**；迁移 §7.1（L223）「B/C 通道若内置 dsh 本体则版本随壳钉死」+ **显式引用官方 SESSION_FORMAT_VERSION=0、无格式兼容承诺姿态**；落地 §9 行 26（L305）验收「旧会话可继续读写、seq 无冲突」 | ✅ 闭环 |
| E1 签名密钥 SOP | H | 大纲级残留 | 可行性 P3（L205）/落地 T5.4（L205）：生成→保管（**私钥备份 + 双人审批 + 明确保管人，Authenticode/notarization 凭据同规则**）→轮换（含旧签名兼容期；**兼容期实现验证——updater 是否支持多 pubkey/双签名过渡——入 P4 插件核实清单**）→泄露处置（吊销+强制更新路径） | ✅ 闭环（兼容期实现为 P4 追踪项，已显式入核实清单） |

### 1.2 轮 2 新发现项（R1–R13 + G1–G6 + risk 相关 N 系列）

| 项 | 轮 2 严重度 | 轮 3 终验证据 | 结论 |
|---|---|---|---|
| R1 可行性 #7 残留 3080 / 修订表缺 A1 行 | L | 可行性 §2 #7（L46）显式删除残留 + 修订表 A1 行（L247） | ✅ |
| R2 token 生命周期 + 校验方错位 + WS/SSE Origin | M | 可行性 §5.6/P1（L169/L203）；落地 X4-④ 扩子任务（L227）+ T4.9 实现方（L173）；X1 日志脱敏（L224） | ✅ |
| R3 stdio 帧标识 + READY 先验证 + schema 校验 | M | 迁移 §4-9（L159）帧前缀（`DSH_EVENT <json>`/长度前缀）+ schema 严格校验 + 输出按不可信输入处理（第三方插件任意代码可伪造 READY/exit 行）；落地 T4.1（L165）按帧解析、exit 以 `Child::try_wait` 为准；T4.4（L168）READY「先验证再导航」升格强制前置；T4.7（L171）`dsh:output` 渲染强制 text（禁 innerHTML） | ✅ |
| R4 `__DSH_SHELL__` 可覆写 + Node 读取机制未定义 | M | 迁移 §5.2 / 落地 T4.6（L170）：接线信号改 **env `DSH_HUB_SHELL=tauri`**（Rust spawn 注入，无往返无竞态）；探针仅作客户端 adapter 提示（非完整性边界）；页面就绪握手（adapter 类型/通道上报，T4.9） | ✅ |
| R5 capabilities 起步集含 core:default | L | 落地 T1.4（L48）+ X4-① 终审标准（L227） | ✅ |
| R6 深链参数规范未列产出物 | L | 落地 X4-⑥（L227）补产出物行 | ✅ |
| R7 卸载后 junction 悬空 + bundles 残留 → 裸 dsh web 启动失败 | M | 落地 T5.1（L202）卸载器清理 `profiles/web/package.json` bundles 条目 + junction（或文档化补救 `dsh plugin --profile web remove @marecgents/dsh-hub`；已核实 resolveBundleDir 双锚点失败 fail loud）；T5.5-⑥（L206）加「**卸载后裸 `dsh web` 可正常启动**」断言 | ✅ |
| R8 SHASUMS 无签名校验 / 哈希登记 / cargo audit DB | L | 迁移 §7.1（L223）/落地 T5.2（L203）：固定版本 + SHASUMS256 + **同源 `.sig` OpenPGP 校验（固定 Node 官方签名密钥指纹；或至少注明 TLS+固定 URL 残余风险）** + 失败中止 + expected-hash 清单入仓/构建产物比对/记入发布记录；T5.5-①（L206）cargo audit 注明 advisory DB 拉取方式（CI 定期 job 或缓存更新） | ✅ |
| R9 T5.1 卸载清单未含 webview-data | L | 落地 T5.1 ②（L202）补 webview-data；X3（L226）备份范围显式声明不含 webview-data | ✅ |
| R10 双通道检测执行细节 | L | 迁移 §4-5（L147）/落地 T4.2（L166）：流水线（枚举自排除当前 PID → PID→监听端口解析 → 逐端口探活）+ 判定规则（枚举命中即默认拒绝+确认，探活失败重试 2-3 次）+ spawn 后二次确认（TOCTOU） | ✅ |
| R11 dsh 版本钉死 + 快照语义 | L/M | 落地 T5.3/§9 行 26/§10-7（L204/L305/L319）：B/C 钉死、A 通道矩阵+快照缓解、显式引用官方姿态、快照落点/校验/保留期 | ✅ |
| R12 E1 SOP 细节 | L | 可行性 P3/落地 T5.4（L205）：私钥备份+双人审批+保管人；兼容期验证入 P4 | ✅ |
| R13 NSIS 覆盖升级路径 | L | 落地 T5.1（L202）：旧版本号检测实现（Uninstall 键 DisplayVersion 或安装目录 version 文件）+ 覆盖安装前先杀 sidecar/主进程（EBUSY 类占用） | ✅ |
| G1–G6（ops） | M/L | 落地 T5.1/T4.2/T4.3/T4.4/T5.3/T5.5-②⑥（L166-206）全部有任务落点与消费方（G4 实现=T4.4，T5.5 为消费方） | ✅ |
| N1/N3/N5/N13/N14/N15/N16（arch，risk 相关） | M/L | 落地附录 C（L412-425）任务级落点：T4.9 主桥服务端、dsh:output 走主桥、env 接线、dev/prod 桥一致性、initialization_script 注入适配器、shell_command 不暴露 invoke、下行清单收敛 | ✅ |

### 1.3 轮 1 中低危复核（轮 2 已确认闭环，本轮抽查一致）

| 项 | 复核 |
|---|---|
| A4 updater 多端点/企业网络 | ✅ 迁移 §7.2（L228）多 endpoints + 降级路径（提示全局 dsh/离线包）+ 代理说明；落地 T5.3（L204） |
| A5 杀软/EDR 误报 | ✅ 落地 T5.4（L205）测试证书先行 + AV 白名单提交；T5.5-③（L206）「首启被安全软件拦截」检查项 |
| A6 平台依赖矩阵 | ✅ 迁移 §7.1（L222）+ 落地 T5.5-③ Win10 无 WebView2 Runtime 隔离机用例 + §9 平台列 |
| B4 localStorage/webview-data | ✅ 落地 X4-⑦（L227）显式配置 `$DSH_HOME/dsh-hub/webview-data` + 备份/清理边界 + localStorage 核实任务 |
| B5 数据敏感度说明 | ✅ 落地 X3（L226）备份内容与敏感度边界（不含 token/密钥，ACL 与现状一致） |
| C2 备份闭环 | ✅ X3/T5.3 备份挂所有升级入口（updater + NSIS 检测旧版本）+ 备份失败中止 + 恢复入口定义 + T5.5-④ 演练 |
| C3 卸载清理边界 | ✅ T5.1 三段清理 + 卸载前杀 sidecar + T5.5-⑥ 卸载冒烟（含 R7/G6 增强断言） |
| D1/D2 回退 | ✅ 回退手册入 P7 + T5.5-⑦ 全链路冒烟（数据双向兼容）+ 语义下沉为快捷方式/启动命令切换 |
| E2 Rust 依赖供应链 | ✅ T5.5-① cargo audit 门禁（advisory DB 拉取方式注明） |
| E3 信任边界声明 | ✅ 落地 T5.6（L207）README 信任模型说明（壳/运行时签名 vs 插件安装信任链） |

---

## 二、安全设计完整性（可执行闭环核验）

| 安全面 | 起步 | 执行 | 验收/门禁 | 闭环 |
|---|---|---|---|---|
| **capabilities 最小授权** | T1.4 显式最小集（无 `core:default`/`shell:default` 全家桶，按 `generate_handler` 实际注册命令枚举，按窗口 label 分组 main vs splash） | X4-① 终审产物 = capabilities 最终清单 + 每权限注释 + 页面/命令/权限三元组对照表；M4 用 `tauri dev` ACL 报错驱动「真实调用点 → 加对应权限」 | 权限集名称并入 P4 核实（开工前）；M1 验收加本地窗口内 IPC 冒烟 | ✅ |
| **token 生命周期** | 生成：Rust 壳每次启动生成（可行性 §5.6 L169） | 下发：经 stdio `init{token}`/env 下发 sidecar；分发：初始化脚本注入 `window.__DSH_BRIDGE_TOKEN__`（**禁止放 URL query**）；校验：**Node host half 端点侧**（T4.9/X4-④，Host 白名单 + Origin + token，WS/SSE upgrade 与 POST 同强度） | 轮换：壳重启即轮换；日志脱敏：X1 明确不打印 token；P1 安全分析含全生命周期 | ✅ |
| **stdio 帧标识** | 协议定帧：`DSH_EVENT <json>` / 长度前缀（迁移 §4-9 L159） | schema 严格校验（未知字段拒绝）；**输出按不可信输入处理**（dsh host 加载第三方插件任意代码，其 console.log 可伪造 READY/exit 行） | READY「先验证再导航」（连接 + HTTP 探测确认是本 sidecar 监听端口后才建窗，T4.4 强制前置）；exit 以 `Child::try_wait` 为准；`dsh:output` 前端渲染强制 text（禁 innerHTML，T4.7） | ✅ |
| **卸载清理** | T5.1 三段清理（①必删安装目录/快捷方式/Uninstall 键 ②可选勾选 `$DSH_HOME/dsh-hub/` 自有文件**含 webview-data** ③默认保留 profiles/sessions） | 卸载器清理本插件装配痕迹（bundles 条目 + junction，R7）；先结束主进程 + sidecar、超时强杀（G6）；升级路径检测旧版本号 + 升级前杀进程（R13） | T5.5-⑥ 卸载冒烟：三段边界断言 + **卸载后裸 `dsh web` 可启动断言** + 运行中卸载用例 | ✅ |
| **供应链** | 固定版本 + 官方源固定（T5.2） | SHASUMS256.txt 校验 + **同源 `.sig` OpenPGP 签名校验（固定官方签名密钥指纹）** + 失败即中止；按需下载运行时**默认关闭、仅显式 opt-in** | expected-hash 清单入仓 + 构建产物比对 + 记入发布记录（verify-tauri-release）；`cargo audit` 入门禁（advisory DB 拉取方式注明）；`tauri signer verify` 验签入 P5；密钥生命周期 SOP（P3/T5.4） | ✅ |

---

## 三、残余风险（显式记录核验）

| 残余风险 | 处置/缓解 | 记录位置（本轮读到） | 显式性 |
|---|---|---|---|
| **A 通道全局 npm dsh 版本不受壳控制**（会话格式无兼容承诺） | dsh 版本兼容矩阵 + 升级前会话只读快照为唯一缓解；显式引用官方「SESSION_FORMAT_VERSION=0、无格式兼容承诺」姿态 | 迁移 §7.1（L223）、落地 §9 行 26 / §10-7（L305/L319） | ✅ 显式记录 |
| **壳自身崩溃守护形态未定** | M4/M5 决策任务：外部轻量 launcher / 计划任务 / 显式声明「手动重启可接受」并**写入验收差异** | 落地 X2 / §9 行 27（L225/L306）、迁移 §4-6 | ✅ 显式记录（决策点 + 验收差异通道） |
| **行为变更决策点**（需用户拍板） | 单实例二次启动聚焦 vs 弹窗拒绝；get_workspace_path 应答方向（M4 前截止）；声音三选一（M3/M4 前）；Explorer 前置激活或声明「不激活」为验收差异；深链 v1 范围外（M5 决策启用路径或显式「v1 不做」） | 落地 §10-5 / T4.8 / §9 行 14/15/25（L293-317）、迁移 §3.3/T3.3 | ✅ 显式记录（含截止日） |
| **插件 API / tao/wry 行为未本地核实** | 动工前前置条件 P4（clone plugins-workspace 核实 7 插件 API）/ P5（tao/wry 三端行为冒烟），均有验收标志 | 可行性 §7.2（L206-207）、迁移 §9.2 | ✅ 显式记录 |
| **CSP 维持 null** | X4-⑤ M5 前评估收紧可行性（dsh 页面内联样式/WebSocket/插件注入），不阻塞主线 | 落地 X4-⑤（L227）、迁移 §9.2-7 | ✅ 显式记录 |
| **安装包体积口径落差** | 壳二进制 ~10MB 与安装包 40-80MB 分口径表述，**删除「安装包 ~10MB」硬验收**；A/B/C 通道 M5 前决策；按需下载默认关闭、opt-in | 落地 §6.3-4 / §10-6（L215/L318）、可行性 §5.1/P2 | ✅ 显式记录 |
| **回退线退役** | 首个正式版发布即移除 webviewjs/koffi（门禁检查），双壳并存仅限 dev-v2 过渡期 | 可行性 §5.5、迁移 §7.3、落地 T5.5-⑨（L206） | ✅ 显式记录 |

**验收差异机制已内建**：落地 §9 表多行允许「显式声明差异」替代行为等价（行 14 Explorer 激活、行 27 壳崩溃守护、T3.4 set_icon 不支持则固定深色图标并更新验收表）——残余风险均有合规出口，不靠静默降级。

---

## 四、三报告一致性终核（risk 相关关键表述抽查）

轮 2 曾点名的一致性缺口已全部收敛，本轮抽查 8 项跨报告关键安全表述一致：

1. 主桥服务端归属：sidecar（Node host half）伺服并校验，Rust 壳不实现 HTTP 桥 — 可行性 §3.4/§5.6 ↔ 迁移 §2.1/§3.1 ↔ 落地 T4.9 ✅
2. 多实例双通道、不依赖固定端口 — 可行性 §2 #7 ↔ 迁移 §3.2/§4-5 ↔ 落地 T4.2/T4.4 ✅
3. stdio 方向标签（notify/applyTheme/applySize 上行，quit 下行，READY/output/exit 事件行）— 可行性 §3.4 ↔ 迁移 §2.1/§4-9 ↔ 落地 T4.1 ✅
4. 接线信号改 env（`DSH_HUB_SHELL=tauri`）— 可行性 §3.4 ↔ 迁移 §5.2 ↔ 落地 T4.1/T4.6 ✅
5. token 会话级语义 + 分发 + 校验方 — 可行性 §5.6/P1 ↔ 落地 X4-④/T4.9 ✅
6. READY「先验证再导航」— 迁移 §4-9/§9.2-11 ↔ 落地 T4.4 ✅
7. 卸载清理与装配痕迹清理（R7）— 迁移 §7.5 ↔ 落地 T5.1/T5.5-⑥ ✅
8. 供应链校验（SHASUMS + .sig + 哈希登记）— 可行性 P2 ↔ 迁移 §7.1 ↔ 落地 T5.2 ✅

未再发现跨报告矛盾残留。

---

## 五、最终结论

**可验收（有条件通过）**。依据：

1. **三轮闭环达成**：轮 1 全部高危/阻断项（A1/A2/B1/B2/B3/C1/E1）与轮 2 全部新发现项（R1–R13、G1–G6、risk 相关 N 系列）在本轮均核验到**任务级落点 + 验收方法 + 门禁条目**，无遗留未处理项；轮 2 点名要求 M1 前修正的 R1/R2/R4 三处已全部落实。
2. **安全设计五大面可执行闭环**：capabilities（显式最小集 → X4-① 终审 → ACL 报错驱动）、token（生成→下发→注入→端点校验→轮换→脱敏）、stdio（帧标识→schema 校验→先验证再导航→text 渲染）、卸载（三段清理+装配痕迹清理+裸 dsh 启动断言+运行中卸载用例）、供应链（SHASUMS256+.sig+expected-hash 清单+cargo audit+密钥 SOP）均为「实现方 + 验收 + 门禁」三重落位。
3. **残余风险全部显式记录**：A 通道 dsh 版本不受控、壳崩溃守护决策、行为变更决策点、P4/P5 待核实、CSP、体积口径、回退线退役——均有处置/缓解/截止日，且「验收差异」写入通道内建。

**验收附条件**（均为报告自身已排期的闭合动作，非新增要求）：
- **动工前**：P4（plugins-workspace 核实）/ P5（tao/wry 行为冒烟）完成；X4 安全产物与 A1 双通道按 T4.2/T4.4/X4 任务落地；
- **里程碑截止日前**：单实例行为、get_workspace_path 方向、声音通道、壳崩溃守护、深链范围等决策点拍板并写入验收差异；CSP 评估（M5 前）；
- **发布门禁**：verify-tauri-release 任一 FAIL 禁止发布（延续根 AGENTS.md 铁律 0）。

**无需再修三报告正文**；若上述附条件在对应里程碑前全部闭合，则风险安全维度达到「可无缝迁移落地」标准。
