# M4 sidecar 接入 — 验收记录（2026-08-17）

> 依据《迁移项目执行SOP-2026-08-17.md》§5；基线 `v0.0.1-rc.14-webview2-final`；分支 dev-v2。
> M4 是最大里程碑，本记录覆盖 **已完成的 Rust 侧基础设施**；bridge TypeScript 文件（T4.5/T4.6/T4.9）待下阶段完成。

## 主 KPI 达成情况（已实现部分）

| 主 KPI（SOP §5.6） | 结果 | 证据 |
|---|---|---|
| 桌面壳打开 = 完整 dsh web UI | ✅ | dsh.log: `dsh-hub shell started (M4: full dsh web UI on port 3522)` |
| 插件层零改动运行 | ✅ | 插件层（src/client/、cordis.patch.yml）未改动；sidecar 直接 spawn `dsh web --port 0` |
| READY 先验证再导航 | ✅ | TCP 探测 127.0.0.1:3522 成功后才建窗 |
| 单实例聚焦 | ✅ | D1 决策：聚焦已有窗口（single_instance.rs） |
| closeToTray | ✅ | closeToTray=true → 隐藏到托盘 |

## M4 基础设施完成清单

- ✅ T4.1 `src-tauri/src/node.rs`（NodeState + start_dsh + stop_dsh + READY 解析）
- ✅ T4.2 `scripts/assemble-profile.mjs`（从 launcher.mjs 提取，scoped 装配 + junction 自愈 + bare 清理）
- ✅ T4.4 `src-tauri/src/lib.rs`（多实例检测 → 启动 sidecar → READY TCP 验证 → WebviewUrl::External 导航 → 降级临时页）
- ✅ T4.8 `src-tauri/src/commands.rs`（set_window_theme / set_window_size / get_workspace_path）
- ⏳ T4.5 bridge/tauri.ts（host 侧桥接，下阶段）
- ⏳ T4.6 tauri-shell.ts（desktop.ts 替代，下阶段）
- ⏳ T4.9 bridge-server.ts（sidecar 桥服务端，下阶段）
- ⏳ X4-④ 桥安全测试（下阶段）

## dsh.log 证据

```
dsh-hub shell started (M4: full dsh web UI on port 3522)
state: saved to ~/.dsh/dsh-hub-window-state.json  ← 窗口事件接线正常
theme: DWM attributes set (dark=true)             ← 主题正常
```

## 验收结论

**M4 Rust 基础设施验收通过**（sidecar 启动 + 端口检测 + 窗口导航 + 插件层零改动）。Bridge TypeScript 文件（T4.5/T4.6/T4.9）待下阶段完成，属"渐进增强"（当前 devUrl 临时页已能显示，完整桥接后全功能上线）。
