{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M1 显式最小授权集——禁用 core:default 全家桶；按真实调用点（ACL 报错驱动）逐项最小化放行",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:default",
    "log:default"
  ]
}
